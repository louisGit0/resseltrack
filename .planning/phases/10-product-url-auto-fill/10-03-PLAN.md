---
phase: 10-product-url-auto-fill
plan: 03
type: execute
wave: 3
depends_on: ["10-02"]
files_modified: []
autonomous: false
requirements: [IMPORT-01]

must_haves:
  truths:
    - "the live POST /products/fetch-url returns JSON (not a 302 redirect to update()), proving correct route ordering (D-07)"
    - "an obviously-internal URL such as http://169.254.169.254/ is rejected by the SSRF guard with a clean French message — no internal endpoint is reached (D-02)"
    - "pasting a public AliExpress product URL pre-fills at least the name field when parseable, otherwise degrades to the French manual-entry message — both are spec-compliant outcomes (D-01)"
    - "auto-fill is non-destructive: a pre-typed name/price is left untouched and every field stays editable (D-06)"
    - "a product saved after using auto-fill is identical in the database to one saved by manual entry — the import path does not bypass validation or add fields"
  artifacts: []
  key_links:
    - from: "live product form"
      to: "/products/fetch-url"
      via: "browser fetch returning JSON"
      pattern: "products/fetch-url"
---

<objective>
Deploy Phase 10 (a normal git push → Vercel build; no schema change, no migration, no new package) and verify the three ROADMAP success criteria plus the headline SSRF control against the live site.

Purpose: The route ordering, SSRF rejection over a real network, and the best-effort E2E flow need a logged-in browser session and the live URL, so they are verified manually per existing project practice (the pure logic is already unit-tested in Plan 10-01).

Output: Confirmation that the live endpoint resolves to fetchUrl(), rejects internal URLs, and delivers the best-effort fill / graceful French fallback.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-product-url-auto-fill/10-VALIDATION.md
@.planning/phases/10-product-url-auto-fill/10-01-SUMMARY.md
@.planning/phases/10-product-url-auto-fill/10-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Deploy and live-verify the import flow (route ordering, SSRF, best-effort E2E)</name>
  <read_first>
    - .planning/ROADMAP.md lines 245-258 — Phase 10 goal and the three Success Criteria.
    - .planning/phases/10-product-url-auto-fill/10-VALIDATION.md lines 47-52 — the manual-only verification list (route ordering, SSRF end-to-end, best-effort UX).
    - Memory: public alias is `resseltrack-nu.vercel.app` (hash URLs are SSO-401).
  </read_first>
  <what-built>
    Plan 10-01 shipped the SSRF-guarded `ProductImportService` (unit-tested). Plan 10-02 shipped the `fetchUrl()` JSON endpoint, the `POST /products/fetch-url` route (ordered before `/products/{id}`), the form URL input/button/data:-URI preview, and the `initUrlAutofill()` handler. This deploy is a normal git push to the default branch → Vercel rebuilds. No migration, no operator step.
  </what-built>
  <how-to-verify>
    1. Push the Phase 10 commits so Vercel deploys; wait for the build to finish, then open the public alias `https://resseltrack-nu.vercel.app` and log in.
    2. ROUTE ORDERING (Success Criterion + Pitfall 1): from the browser devtools console while logged in, POST form-encoded to `/products/fetch-url` with `url=https://example.com` and the page's `_csrf` value (read `document.querySelector('input[name="_csrf"]').value`). Expect a JSON body (`{"ok":...}`), NOT a 302 redirect to a product page and NOT "Produit introuvable". This proves the route resolves to `fetchUrl()` not `update()`.
    3. SSRF (D-02): repeat the POST with `url=http://169.254.169.254/` (cloud metadata) and again with `url=http://127.0.0.1/`. Expect `ok:false` with a clean French message each time and NO internal content returned.
    4. BEST-EFFORT E2E (Success Criteria 1 & 2): open the product create form, paste a current public AliExpress product URL into the import field, and click "Remplir depuis URL". Expect EITHER the name field pre-filled (and price/preview when parseable) OR the French message "Remplissage automatique indisponible — veuillez saisir manuellement". Both are PASS — AliExpress is anti-bot/JS-only, so the fallback message is the expected common outcome; do NOT treat it as a failure.
    5. NON-DESTRUCTIVE (D-06): type a name manually BEFORE clicking, then click — confirm the typed name is NOT overwritten and all fields remain editable.
    6. PARITY (Success Criterion 3): finish saving the product and confirm it persists like any manual product (no extra fields, validation applied normally).
    7. CONTINGENCY: if the live POST intermittently times out (504) because the page fetch + image fetch brush Vercel's function duration limit, set `maxDuration` (~15s) for the function in `vercel.json` and redeploy, then re-run step 4.
  </how-to-verify>
  <acceptance_criteria>
    - The live `/products/fetch-url` POST returns JSON, not a 302 — route ordering confirmed.
    - `http://169.254.169.254/` and `http://127.0.0.1/` are rejected with `ok:false` + a French message; no internal data leaks.
    - The AliExpress paste either pre-fills the name or shows the French manual-entry message; both are accepted.
    - A pre-typed name survives auto-fill (non-destructive); all fields stay editable.
    - The saved product is identical to a manually-entered one (no bypassed validation, no extra fields).
  </acceptance_criteria>
  <resume-signal>Type "approved" once the route returns JSON, internal URLs are rejected, and the best-effort fill OR French fallback works — or describe the observed failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live user URL → server fetch | SSRF surface — verified live that internal URLs are rejected |
| browser → POST /products/fetch-url | CSRF + route-ordering surface — verified the endpoint returns JSON not a redirect |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Information Disclosure / EoP | live SSRF to metadata/loopback | mitigate | Live check confirms `169.254.169.254` and `127.0.0.1` are rejected with a French message (guard from Plan 10-01) |
| T-10-SH | EoP | live route shadowing | mitigate | Live check confirms the POST returns JSON not a 302 to update() |
| T-10-06 | Spoofing | live CSRF | mitigate | The endpoint requires the form-encoded `_csrf`; exercised via the logged-in session |

Supply-chain (T-10-SC): N/A — no package installed; deploy is a plain git push.
</threat_model>

<verification>
- Live route returns JSON (not 302).
- Internal URLs rejected with a French message.
- Best-effort fill or graceful French fallback; non-destructive; saved-product parity.
</verification>

<success_criteria>
- All three ROADMAP Success Criteria pass on the live site.
- The SSRF guard rejects internal URLs over a real network.
- IMPORT-01 is verified end-to-end with best-effort semantics intact.
</success_criteria>

<output>
Create `.planning/phases/10-product-url-auto-fill/10-03-SUMMARY.md` when done.
</output>
