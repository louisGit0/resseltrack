---
phase: 08-suppliers-and-product-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Controllers/ProductController.php
autonomous: true
requirements: [OPS-06]
must_haves:
  truths:
    - "Deleting a product removes its cover image and every gallery image from Cloudinary, best-effort"
    - "A Cloudinary failure is logged via error_log() and never blocks the DB delete — the product rows are always removed"
    - "Legacy non-Cloudinary paths (/assets/uploads/...) cause no error (CloudinaryStorage::delete no-ops on them)"
  artifacts:
    - path: "src/Controllers/ProductController.php"
      provides: "destroy() collects cover+gallery paths before delete, dedupes, then best-effort Cloudinary purge"
      contains: "array_unique"
  key_links:
    - from: "ProductController::destroy()"
      to: "CloudinaryStorage::delete()"
      via: "best-effort try/catch loop over deduped paths AFTER Product::delete()"
      pattern: "error_log\\('Cloudinary delete failed"
    - from: "ProductController::destroy()"
      to: "ProductImage::allForProduct()"
      via: "gallery path collection BEFORE the cascading delete"
      pattern: "allForProduct"
---

<objective>
Close the orphaned-image bug (OPS-06 / STATE.md tracked debt): make `ProductController::destroy()` purge the product's cover image and every gallery image from Cloudinary, best-effort, mirroring the existing `deleteImage()` try/catch pattern. A Cloudinary failure is logged and never blocks the DB delete.

Purpose: Today deleting a product leaves its Cloudinary assets orphaned forever.
Output: Modified `src/Controllers/ProductController.php`. Fully independent of the supplier work — runs in Wave 1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-suppliers-and-product-cleanup/08-RESEARCH.md
@.planning/phases/08-suppliers-and-product-cleanup/08-PATTERNS.md
@.planning/phases/08-suppliers-and-product-cleanup/08-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite ProductController::destroy() to purge Cloudinary objects best-effort</name>
  <read_first>
    - src/Controllers/ProductController.php — read the full file. Specifically the current `destroy()` action (~lines 295-301), the `deleteImage()` best-effort try/catch loop (~lines 376-381) which is the exact pattern to replicate, the existing imports for `App\Models\ProductImage` (~line 10) and `App\Services\CloudinaryStorage` (~line 14), and how `setCover`/`uploadImages` couple cover↔gallery (~lines 342-349)
    - src/Models/ProductImage.php — confirm the `allForProduct($userId, $productId)` signature returns rows with a `path` key
    - src/Services/CloudinaryStorage.php — `delete()` (~lines 59-81) and `derivePublicId()` (~lines 135-144) confirming legacy/non-Cloudinary paths no-op safely
    - .planning/phases/08-suppliers-and-product-cleanup/08-PATTERNS.md §"src/Controllers/ProductController.php (controller, file-I/O purge) — MODIFIED" — copy-ready destroy() body
    - .planning/phases/08-suppliers-and-product-cleanup/08-RESEARCH.md §"Common Pitfalls" (Pitfalls 2 & 3: cover-is-also-gallery dedupe; legacy paths)
  </read_first>
  <action>
    Rewrite `destroy(array $params)`: keep `Csrf::validate()` as the first line. Then, BEFORE deleting, collect Cloudinary paths into `$paths`: call `$this->products->find($pid, $userId)` and push `image_path` (cover) when non-empty; loop `(new ProductImage())->allForProduct($userId, $pid)` pushing each `path` (gallery). Dedupe with `array_values(array_unique(array_filter($paths)))` (Pitfall 2 — cover is usually also a gallery row). Then call the existing `$this->products->delete($pid, $userId)` (cascade removes product_images/purchases/sales). Then loop the deduped paths through `(new CloudinaryStorage())->delete($path)` inside a `try { ... } catch (\Throwable $e) { error_log('Cloudinary delete failed for path ' . $path . ': ' . $e->getMessage()); }` — best-effort, never re-throw, never block (D-11). Keep the existing success flash + `redirect('/products')`. No new `use` statements (ProductImage + CloudinaryStorage already imported). Do NOT change CUMP/stock/fee logic or any other action.
  </action>
  <verify>
    <automated>php -l src/Controllers/ProductController.php</automated>
  </verify>
  <acceptance_criteria>
    - `php -l src/Controllers/ProductController.php` reports "No syntax errors detected".
    - Source assertion: within `destroy()`, the call to `$this->products->find(` (path collection) appears textually BEFORE `$this->products->delete(` (the cascade). 
    - Source assertion: `grep -c "array_unique" src/Controllers/ProductController.php` >= 1 (dedupe present in destroy()).
    - Source assertion: `destroy()` contains a `catch (\Throwable` that calls `error_log(` and does NOT re-throw or `exit` (the DB delete is never blocked by a purge failure).
    - Source assertion: no new `use App\` import lines were added (ProductImage/CloudinaryStorage already imported).
    - `vendor/bin/phpunit` stays green (ProfitCalculator untouched).
  </acceptance_criteria>
  <done>destroy() collects cover+gallery before delete, dedupes, deletes, then purges Cloudinary best-effort with logged failures that never block.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → ProductController::destroy (POST) | state-changing delete; CSRF + ownership boundary |
| ProductController → Cloudinary (signed REST) | external service; failures must not affect data integrity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Tampering | POST /products/{id}/delete | mitigate | `Csrf::validate()` first line of destroy() (unchanged) |
| T-08-05 | Elevation of Privilege | deleting another user's product | mitigate | `find($pid,$userId)` + `delete($pid,$userId)` scoped `:uid`; non-owned find returns null → no paths, scoped delete affects 0 rows |
| T-08-06 | Denial of Service | Cloudinary outage blocking deletes | mitigate | best-effort try/catch on `\Throwable`; DB delete runs before purge and is never rolled back (D-11) |
| T-08-07 | Tampering | malicious/legacy stored path sent to Cloudinary | accept | `CloudinaryStorage::delete()` → `derivePublicId()` returns '' for non-`/image/upload/` paths and early-returns; no injection surface |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (RESEARCH audit: N/A) |
</threat_model>

<verification>
- `php -l src/Controllers/ProductController.php` clean.
- All source assertions above pass.
- `vendor/bin/phpunit` green.
- (Live purge confirmation is deferred to the operator verification in Plan 06.)
</verification>

<success_criteria>
- Product deletion purges cover + gallery from Cloudinary best-effort; failures are logged and never block; DB rows always removed.
</success_criteria>

<output>
Create `.planning/phases/08-suppliers-and-product-cleanup/08-02-SUMMARY.md` when done
</output>
