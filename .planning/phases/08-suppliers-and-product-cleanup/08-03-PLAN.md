---
phase: 08-suppliers-and-product-cleanup
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/Models/Supplier.php
  - src/Controllers/SupplierController.php
  - public/index.php
  - src/Views/layout.php
autonomous: true
requirements: [SUP-01]
must_haves:
  truths:
    - "GET /suppliers reaches SupplierController::index and lists the authenticated user's suppliers with a linked-orders count"
    - "Supplier create/edit/delete are user-scoped; a non-owned id on edit/update/destroy redirects with a 'not found' flash"
    - "Every supplier POST validates CSRF; name is required; rating must be 1-5 or null"
    - "The Activité sidebar (and mobile offcanvas) shows a Fournisseurs nav entry"
  artifacts:
    - path: "src/Models/Supplier.php"
      provides: "per-user CRUD + allForUser() with linked-orders COUNT (D-07)"
      contains: "COUNT(o.id) AS orders_count"
    - path: "src/Controllers/SupplierController.php"
      provides: "index/create/store/edit/update/destroy + validate() (no show action)"
      exports: ["index", "store", "update", "destroy"]
    - path: "public/index.php"
      provides: "6 supplier routes (create before {id}/edit) + SupplierController import"
      contains: "SupplierController"
    - path: "src/Views/layout.php"
      provides: "Fournisseurs nav link in the Activité section"
      contains: "/suppliers"
  key_links:
    - from: "public/index.php"
      to: "SupplierController"
      via: "router get/post registration"
      pattern: "\\[SupplierController::class"
    - from: "SupplierController::edit/update/destroy"
      to: "Supplier::find($id, Auth::id())"
      via: "ownership-or-redirect guard"
      pattern: "find\\("
    - from: "Supplier::allForUser()"
      to: "orders.supplier_id"
      via: "LEFT JOIN orders COUNT GROUP BY"
      pattern: "LEFT JOIN orders"
---

<objective>
SUP-01 backend: the per-user `Supplier` model, the `SupplierController` CRUD controller, the route registrations, and the navigation entry. This makes the Fournisseurs feature reachable and persistent. The views and star widget that render this data are Plan 04.

Purpose: Establishes the controller/model/routes/nav plumbing for supplier management, all cloned 1:1 from existing Order/Product analogs.
Output: New `Supplier.php`, `SupplierController.php`; modified `public/index.php`, `layout.php`.
Depends on Plan 01 (suppliers table must exist for queries to succeed at runtime — applied by the operator in Plan 06).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-suppliers-and-product-cleanup/08-RESEARCH.md
@.planning/phases/08-suppliers-and-product-cleanup/08-PATTERNS.md
@.planning/phases/08-suppliers-and-product-cleanup/08-CONTEXT.md

<interfaces>
<!-- Contracts the executor builds against; verified from the analogs. -->
From src/Models/Order.php (the template Supplier mirrors):
- final class with `private PDO $db;`, ctor `$this->db = Database::connection();`
- `allForUser(int $userId): array` (LEFT JOIN + COUNT + GROUP BY + ORDER BY shape)
- `find(int $id, int $userId): ?array` (returns null when not owned)
- `create(int $userId, array $data): int`, `update(int $id, int $userId, array $data): void`, `delete(int $id, int $userId): void`

From src/Core (used by the controller):
- `App\Core\Auth::require()` (ctor gate), `App\Core\Auth::id(): ?int`
- `App\Core\Csrf::validate()` (first line of every POST action)
- `App\Core\Controller`: `view($tpl, $data, $title)`, `redirect($path)`, `flash($type,$msg)`, `flashErrors($errors,$old)`, `pullErrors()`/old-input
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create the Supplier model (per-user CRUD + linked-orders count)</name>
  <read_first>
    - src/Models/Order.php — the whole file is the template: `final`, `private PDO $db`, ctor `Database::connection()`, `allForUser()` LEFT JOIN+COUNT+GROUP BY (~lines 22-38), `find()` ownership (~lines 40-48), `create()`/`update()`/`delete()` (~lines 64-113), and the `$data['x'] ?: null` optional-column idiom
    - .planning/phases/08-suppliers-and-product-cleanup/08-PATTERNS.md §"src/Models/Supplier.php (model, CRUD) — NEW" — copy-ready method bodies
  </read_first>
  <action>
    Create `src/Models/Supplier.php`: `declare(strict_types=1);`, namespace `App\Models`, `use App\Core\Database; use PDO;`, `final class Supplier`. Implement:
    - `allForUser(int $userId): array` — `SELECT s.*, COUNT(o.id) AS orders_count FROM suppliers s LEFT JOIN orders o ON o.supplier_id = s.id AND o.user_id = s.user_id WHERE s.user_id = :uid GROUP BY s.id ORDER BY s.name ASC` (D-07, D-09 default sort by name).
    - `find(int $id, int $userId): ?array` — `SELECT * FROM suppliers WHERE id = :id AND user_id = :uid LIMIT 1`, return `$row ?: null`.
    - `create(int $userId, array $data): int` — INSERT columns `(user_id, name, url, rating, comment)`; bind `url => $data['url'] ?: null`, `rating => $data['rating']` (already int|null from controller), `comment => $data['comment'] ?: null`; return `(int)$this->db->lastInsertId()`.
    - `update(int $id, int $userId, array $data): void` — `UPDATE suppliers SET name=:name, url=:url, rating=:rating, comment=:comment WHERE id=:id AND user_id=:uid` (mirror Order::update).
    - `delete(int $id, int $userId): void` — `DELETE FROM suppliers WHERE id=:id AND user_id=:uid` (D-08 unlink handled by the FK ON DELETE SET NULL from Plan 01 — no manual UPDATE orders).
    All SQL prepared statements; all queries `:uid`-scoped.
  </action>
  <verify>
    <automated>php -l src/Models/Supplier.php</automated>
  </verify>
  <acceptance_criteria>
    - `php -l src/Models/Supplier.php` clean.
    - Source: file opens with `declare(strict_types=1);` and declares `final class Supplier`.
    - Source: `grep -c "COUNT(o.id) AS orders_count" src/Models/Supplier.php` returns 1; the same query contains `LEFT JOIN orders` and `WHERE s.user_id = :uid`.
    - Source: `find()` returns `?array` and scopes `WHERE id = :id AND user_id = :uid`.
    - Source: every method binds `:uid` (no unscoped query). `grep -c ":uid" src/Models/Supplier.php` >= 5.
    - Source: no manual `UPDATE orders` in delete().
  </acceptance_criteria>
  <done>Supplier model mirrors Order.php: user-scoped CRUD + linked-orders COUNT; FK handles unlink-on-delete.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create SupplierController (CRUD) and register routes + nav</name>
  <read_first>
    - src/Controllers/ProductController.php — CRUD control flow to clone: `index` style, `create()`/`store()`/`edit()`/`update()`/`destroy()` (~lines 237-301), `validate()` (~lines 411-447), ownership-or-redirect (~lines 263-267), constructor `Auth::require()` (~line 22), model-as-property convention
    - src/Controllers/OrderController.php — the minimal `index()` shape (~lines 39-44) to mirror (no sort/filter/pagination needed)
    - public/index.php — read the `use` import block (~lines 12-19) and the Orders route block (~lines 170-177) with the "static before parameterized" ordering comment
    - src/Views/layout.php — the `$navHtml` closure (~lines 13-54), the Activité `<nav>` section (~lines 25-31), and the `$isActive` helper (~lines 5-10)
    - .planning/phases/08-suppliers-and-product-cleanup/08-PATTERNS.md §"SupplierController", §"public/index.php", §"layout.php" — copy-ready excerpts incl. validate() with rating-range check
  </read_first>
  <action>
    Create `src/Controllers/SupplierController.php`: `declare(strict_types=1);`, namespace `App\Controllers`, `use App\Core\Auth; use App\Core\Controller; use App\Core\Csrf; use App\Models\Supplier;`, `final class SupplierController extends Controller`. Ctor: `Auth::require();` then `$this->suppliers = new Supplier();` (private property). Actions:
    - `index()` → `view('suppliers/index', ['suppliers' => $this->suppliers->allForUser(Auth::id())], 'Fournisseurs')`.
    - `create()` → pull errors/old, `view('suppliers/form', ['supplier'=>null,'errors'=>$errors,'old'=>$old], 'Nouveau fournisseur')`.
    - `store()` → `Csrf::validate()`; `$data=$this->validate()`; if null `redirect('/suppliers/create')`; `$this->suppliers->create(Auth::id(), $data)`; flash success; `redirect('/suppliers')`.
    - `edit(array $params)` → `find((int)$params['id'], Auth::id())`; if null flash 'danger' 'Fournisseur introuvable.' + `redirect('/suppliers')`; else `view('suppliers/form', ['supplier'=>$row, ...], 'Modifier le fournisseur')`.
    - `update(array $params)` → `Csrf::validate()`; re-check `find()` ownership (redirect if null); `$data=$this->validate()` (redirect to `/suppliers/{id}/edit` if null); `$this->suppliers->update($id, Auth::id(), $data)`; flash; `redirect('/suppliers')`.
    - `destroy(array $params)` → `Csrf::validate()`; `$this->suppliers->delete((int)$params['id'], Auth::id())`; flash; `redirect('/suppliers')`.
    - `private validate(): ?array` — trim/cast `name`, `url`, `comment`; `rating = $rawRating === '' ? null : (int)$rawRating`; `$errors['name']` when empty ('Le nom du fournisseur est requis.'); `$errors['rating']` when not null and outside 1..5 ('La note doit être comprise entre 1 et 5.'); on errors `flashErrors($errors, ['name'=>...,'url'=>...,'rating'=>$rawRating,'comment'=>...])` + return null; else return cleaned array. URL handling: trim + store null if empty, mirroring `order_url` (do NOT add filter_var — matches existing convention). NO `show()` action / no `GET /suppliers/{id}` route (D-09 table only).
    Then in public/index.php: add `use App\Controllers\SupplierController;` to the import block, and after the Orders route block add the 6 routes — `get /suppliers` index, `get /suppliers/create` create, `get /suppliers/{id}/edit` edit, `post /suppliers` store, `post /suppliers/{id}` update, `post /suppliers/{id}/delete` destroy (create registered before {id}/edit).
    Then in layout.php: add one nav link `<a class="nav-link <?= $isActive('/suppliers') ?>" href="/suppliers"><i class="bi bi-truck"></i> Fournisseurs</a>` immediately after the Ventes link inside the Activité `<nav>` (D-10). The closure renders into both sidebar and offcanvas automatically.
  </action>
  <verify>
    <automated>php -l src/Controllers/SupplierController.php && php -l public/index.php && php -l src/Views/layout.php</automated>
  </verify>
  <acceptance_criteria>
    - All three `php -l` checks report "No syntax errors detected".
    - Source: SupplierController opens with `declare(strict_types=1);`, is `final`, `extends Controller`, ctor calls `Auth::require()`.
    - Source: every POST action (`store`, `update`, `destroy`) has `Csrf::validate()` as its first statement.
    - Source: `edit`, `update`, `destroy` each guard via `$this->suppliers->find(` with an `Auth::id()` argument (ownership).
    - Source: `validate()` rejects empty `name` and out-of-range `rating` (contains both error strings); no `filter_var` URL validation added.
    - Source: NO `function show(` and `grep -c "/suppliers/{id}'" public/index.php` for a bare GET show returns 0 (only index/create/{id}/edit GETs).
    - Source: `grep -c "\[SupplierController::class" public/index.php` returns 6; `/suppliers/create` route is registered before `/suppliers/{id}/edit`.
    - Source: `grep -c "/suppliers" src/Views/layout.php` >= 1 (single nav line; closure renders twice).
  </acceptance_criteria>
  <done>SupplierController CRUD + validate() created; 6 routes registered with correct ordering and no show route; Fournisseurs nav entry added once.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → SupplierController (GET/POST) | authenticated user input + path id crosses here |
| SupplierController → Supplier model → MySQL | all SQL parameterized + `:uid`-scoped |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-08 | Elevation of Privilege | IDOR on /suppliers/{id}/edit\|update\|delete | mitigate | `Supplier::find($id, Auth::id())` ownership-or-redirect on edit/update/destroy; delete/update SQL scoped `WHERE id AND user_id` (V4 Access Control) |
| T-08-09 | Tampering | CSRF on supplier create/update/delete | mitigate | `Csrf::validate()` first line of every POST action (V5) |
| T-08-10 | Tampering | SQL injection via name/url/rating/comment | mitigate | PDO prepared statements, `ATTR_EMULATE_PREPARES=false`, all values bound — never interpolated |
| T-08-11 | Tampering | invalid rating (e.g. 99, -1) persisted | mitigate | server-side `validate()` enforces rating ∈ {null,1..5} (V5 Input Validation) |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (RESEARCH audit: N/A) |
</threat_model>

<verification>
- All `php -l` checks clean; all source assertions pass.
- `vendor/bin/phpunit` green.
- (Views referenced by index()/create()/edit() are created in Plan 04; runtime smoke is the Wave-5 operator step.)
</verification>

<success_criteria>
- Supplier model + controller + routes + nav implemented per D-05/D-07/D-08/D-09/D-10; CSRF + ownership + input validation enforced; no show route.
</success_criteria>

<output>
Create `.planning/phases/08-suppliers-and-product-cleanup/08-03-SUMMARY.md` when done
</output>
