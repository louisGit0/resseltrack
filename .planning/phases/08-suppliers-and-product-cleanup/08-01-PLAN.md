---
phase: 08-suppliers-and-product-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sql/schema.sql
  - src/Core/Schema.php
autonomous: true
requirements: [SUP-01, SUP-02]
must_haves:
  truths:
    - "Re-running php bin/migrate.php is idempotent: no Duplicate column / table-exists errors on a second run"
    - "A fresh database volume gets a suppliers table from sql/schema.sql"
    - "An existing orders table gains a nullable supplier_id FK column with ON DELETE SET NULL"
  artifacts:
    - path: "sql/schema.sql"
      provides: "CREATE TABLE IF NOT EXISTS suppliers block for fresh installs"
      contains: "CREATE TABLE IF NOT EXISTS suppliers"
    - path: "src/Core/Schema.php"
      provides: "suppliers table + guarded orders.supplier_id ALTER inside Schema::ensure()"
      contains: "SHOW COLUMNS FROM orders LIKE 'supplier_id'"
  key_links:
    - from: "src/Core/Schema.php"
      to: "orders.supplier_id"
      via: "SHOW COLUMNS guard then ALTER ADD COLUMN + FK"
      pattern: "fk_orders_supplier"
    - from: "src/Core/Schema.php"
      to: "suppliers.user_id"
      via: "FK to users ON DELETE CASCADE"
      pattern: "fk_suppliers_user"
---

<objective>
Add the additive, idempotent schema for Phase 8: a new `suppliers` table and a nullable `orders.supplier_id` foreign key. This is the data foundation for SUP-01 (supplier CRUD) and SUP-02 (order↔supplier link). The schema is purely additive and backward-compatible with the live v1.0 code, which is what allows the operator to run the migration before deploying new code (Wave 5).

Purpose: Without these DDL changes, every supplier query and the order supplier_id write fail with "table not found"/"unknown column".
Output: Modified `sql/schema.sql` and `src/Core/Schema.php`. No DDL runs per-request — the actual Aiven migration is the operator step in Plan 06.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-suppliers-and-product-cleanup/08-RESEARCH.md
@.planning/phases/08-suppliers-and-product-cleanup/08-PATTERNS.md
@.planning/phases/08-suppliers-and-product-cleanup/08-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add suppliers table + guarded orders.supplier_id FK to Schema::ensure()</name>
  <read_first>
    - src/Core/Schema.php — read the full file; specifically the `fk_purchases_order` guarded block (~lines 82-95) which is the exact "add nullable FK column to an existing table, idempotently" analog, and the `product_images` CREATE TABLE IF NOT EXISTS block (~lines 53-68) for the new-table style (KEY + FK + InnoDB/utf8mb4 footer)
    - .planning/phases/08-suppliers-and-product-cleanup/08-PATTERNS.md §"src/Core/Schema.php (config / DDL) — MODIFIED" — copy-ready DDL excerpts
    - .planning/phases/08-suppliers-and-product-cleanup/08-RESEARCH.md §"Pattern 3: Idempotent additive migration" — why the SHOW COLUMNS guard is mandatory
  </read_first>
  <action>
    Inside `Schema::ensure()`, add two blocks IN THIS ORDER (suppliers table BEFORE the orders FK so the FK target exists):
    (1) `CREATE TABLE IF NOT EXISTS suppliers` with columns: `id INT UNSIGNED NOT NULL AUTO_INCREMENT`, `user_id INT UNSIGNED NOT NULL`, `name VARCHAR(150) NOT NULL`, `url VARCHAR(500) NULL`, `rating TINYINT UNSIGNED NULL` (1..5, NULL = unrated), `comment TEXT NULL`, `created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP`, `updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP`, `PRIMARY KEY (id)`, `KEY idx_suppliers_user (user_id)`, and `CONSTRAINT fk_suppliers_user FOREIGN KEY (user_id) REFERENCES users (id) ON DELETE CASCADE`; footer `ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci`. Mirror the `fk_pimages_user` FK style.
    (2) A `SHOW COLUMNS FROM orders LIKE 'supplier_id'` guard; only when absent, `ALTER TABLE orders ADD COLUMN supplier_id INT UNSIGNED NULL AFTER user_id`, then a second `ALTER TABLE orders ADD KEY idx_orders_supplier (supplier_id), ADD CONSTRAINT fk_orders_supplier FOREIGN KEY (supplier_id) REFERENCES suppliers (id) ON DELETE SET NULL` (D-03 nullable / D-08 unlink-on-delete). This is an exact mirror of the `fk_purchases_order` guard shape. Column types per RESEARCH assumption A2 (match existing `orders.supplier` width 150, `order_url` width 500).
    Do NOT add `supplier_id` inside any `schema.sql` CREATE TABLE orders block — that is a no-op on the live DB (Anti-Pattern). The guarded Schema::ensure() ALTER is the single source for the column.
  </action>
  <verify>
    <automated>php -l src/Core/Schema.php</automated>
  </verify>
  <acceptance_criteria>
    - `php -l src/Core/Schema.php` reports "No syntax errors detected".
    - Source assertion: `grep -c "CREATE TABLE IF NOT EXISTS suppliers" src/Core/Schema.php` returns 1.
    - Source assertion: `grep -c "SHOW COLUMNS FROM orders LIKE 'supplier_id'" src/Core/Schema.php` returns 1.
    - Source assertion: `grep -c "fk_orders_supplier" src/Core/Schema.php` returns 1, and the same line/block contains `ON DELETE SET NULL`.
    - Source assertion: `grep -c "fk_suppliers_user" src/Core/Schema.php` returns 1, referencing `users (id) ON DELETE CASCADE`.
    - The suppliers CREATE TABLE block appears textually BEFORE the `supplier_id` ALTER block in the file.
  </acceptance_criteria>
  <done>suppliers table + guarded orders.supplier_id FK present in Schema::ensure(), ordered correctly, file parses.</done>
</task>

<task type="auto">
  <name>Task 2: Add the suppliers CREATE TABLE block to sql/schema.sql for fresh installs</name>
  <read_first>
    - sql/schema.sql — read the existing `orders` CREATE TABLE IF NOT EXISTS block for the in-file style (PRIMARY KEY, KEY, CONSTRAINT, `ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;`)
    - .planning/phases/08-suppliers-and-product-cleanup/08-PATTERNS.md §"sql/schema.sql (config / DDL) — MODIFIED"
  </read_first>
  <action>
    Add a `CREATE TABLE IF NOT EXISTS suppliers (...)` block to sql/schema.sql with the same columns and constraints as Task 1 (id, user_id, name VARCHAR(150), url VARCHAR(500) NULL, rating TINYINT UNSIGNED NULL, comment TEXT NULL, created_at, updated_at, PK, idx_suppliers_user, fk_suppliers_user → users ON DELETE CASCADE), matching the in-file style of the `orders` block. Place it after the `orders` table block so the FK target (users) and ordering reads naturally. Do NOT add `supplier_id` to the in-file `orders` block (it diverges from the live table and never applies to existing DBs). bin/migrate.php runs schema.sql first then Schema::ensure(), so the table exists before the guarded FK ALTER runs.
  </action>
  <verify>
    <automated>grep -c "CREATE TABLE IF NOT EXISTS suppliers" sql/schema.sql</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "CREATE TABLE IF NOT EXISTS suppliers" sql/schema.sql` returns 1.
    - Source assertion: the new block contains `rating TINYINT UNSIGNED NULL`, `name VARCHAR(150)`, and `fk_suppliers_user`.
    - Source assertion: `grep -c "supplier_id" sql/schema.sql` returns 0 (supplier_id is NOT added to schema.sql per the anti-pattern).
    - Block ends with `ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;` matching the orders block.
  </acceptance_criteria>
  <done>sql/schema.sql contains an idempotent suppliers table block; supplier_id intentionally absent from schema.sql.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator CLI → Aiven MySQL | DDL applied by `bin/migrate.php` (Plan 06); no per-request DDL (DB-03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | non-idempotent ALTER re-run | mitigate | `SHOW COLUMNS FROM orders LIKE 'supplier_id'` guard prevents Duplicate-column on re-run (mirror of fk_purchases_order) |
| T-08-02 | Denial of Service | DDL in request path | mitigate | No DDL at runtime — `Database::connection()` does not call `Schema::ensure()` (DB-03); migration is the gated CLI operator step in Plan 06 |
| T-08-03 | Information Disclosure | cross-user supplier data leakage at schema level | mitigate | suppliers carries `user_id` + `fk_suppliers_user ON DELETE CASCADE`; data isolation enforced by model `:uid` scoping (Plan 03) |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase — RESEARCH Package Legitimacy Audit: "Not applicable — zero external packages". No supply-chain surface. |
</threat_model>

<verification>
- `php -l src/Core/Schema.php` clean.
- Both grep source assertions pass (suppliers table present in both files; supplier_id only in Schema.php).
- `vendor/bin/phpunit` stays green (no logic touched).
</verification>

<success_criteria>
- suppliers table defined in both sql/schema.sql and Schema::ensure(); orders.supplier_id added only via the guarded Schema::ensure() ALTER with ON DELETE SET NULL; everything idempotent on re-run.
</success_criteria>

<output>
Create `.planning/phases/08-suppliers-and-product-cleanup/08-01-SUMMARY.md` when done
</output>
