---
phase: 08-suppliers-and-product-cleanup
plan: 04
type: execute
wave: 3
depends_on: ["08-03"]
files_modified:
  - src/Views/suppliers/index.php
  - src/Views/suppliers/form.php
  - public/assets/js/app.js
  - public/assets/css/style.css
autonomous: true
requirements: [SUP-01]
must_haves:
  truths:
    - "The /suppliers page renders a full table: name, clickable URL, rating stars, comment, linked-orders count, edit/delete actions"
    - "Unrated suppliers show a neutral placeholder; rated suppliers show filled/empty stars"
    - "The create/edit form lets the user set name, URL, a clickable 1-5 star rating (clearable to unrated), and a comment"
    - "Delete uses the existing confirm modal; all dynamic output is escaped with e()"
  artifacts:
    - path: "src/Views/suppliers/index.php"
      provides: "supplier list table + delete form with data-confirm (D-09)"
      contains: "data-confirm"
    - path: "src/Views/suppliers/form.php"
      provides: "create/edit form with name/url/comment + star widget hidden input (D-05/D-06)"
      contains: "name=\"rating\""
    - path: "public/assets/js/app.js"
      provides: "initStarRating() reusable clickable-star widget (reused by Phase 9)"
      contains: "initStarRating"
  key_links:
    - from: "src/Views/suppliers/index.php"
      to: "app.js form[data-confirm] handler"
      via: "delete form data-confirm attribute (no new JS)"
      pattern: "data-confirm"
    - from: "src/Views/suppliers/form.php"
      to: "app.js initStarRating()"
      via: "hidden input name=rating + clickable bi-star buttons"
      pattern: "name=\"rating\""
---

<objective>
SUP-01 UI: the supplier list table, the create/edit form, and a reusable clickable star-rating widget (hidden input + bi-star toggle) added to app.js (and minimal CSS). The widget is intentionally self-contained so Phase 9 (product rating) reuses it.

Purpose: Renders the data and CRUD surface that Plan 03's controller serves.
Output: New `suppliers/index.php`, `suppliers/form.php`; modified `app.js`, `style.css`.
Depends on Plan 03 (the controller passes `$suppliers`/`$supplier`/`$errors`/`$old` to these views and the routes must exist). Owns the app.js star-widget edit so the SUP-02 toggle (Plan 05) sequences after it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-suppliers-and-product-cleanup/08-RESEARCH.md
@.planning/phases/08-suppliers-and-product-cleanup/08-PATTERNS.md
@.planning/phases/08-suppliers-and-product-cleanup/08-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build the suppliers list table and create/edit form views</name>
  <read_first>
    - src/Views/products/index.php — page header (~lines 20-30), empty-state block (~lines 101-115), the delete-form + data-confirm pattern (~lines 211-215), the edit-link button (~line 210), and the safe clickable-URL/reorder link (~line 194)
    - src/Views/products/form.php — top docblock + `$isEdit`/`$action`/`$val` helper (~lines 1-6), CSRF field (~line 18), name field + is-invalid feedback (~lines 7-27), comment textarea (~lines 35-38), submit/cancel buttons (~lines 94-99)
    - src/Views/orders/show.php — the safe external link convention (escaped href + target=_blank rel=noopener)
    - .planning/phases/08-suppliers-and-product-cleanup/08-PATTERNS.md §"suppliers/index.php" and §"suppliers/form.php" — copy-ready excerpts
  </read_first>
  <action>
    Create `src/Views/suppliers/index.php` with `<?php /** @var array $suppliers */ ?>` docblock: page header (eyebrow "Carnet", title "Fournisseurs", a "Nouveau fournisseur" button linking `/suppliers/create`). Wrap the body in an empty-state guard `if (empty($suppliers))` cloned from products/index.php. When non-empty, render a full Bootstrap table (D-09) with columns: name (`e($s['name'])`), clickable URL (escaped href + `target="_blank" rel="noopener"`, or a muted "—" when empty), rating (render `bi-star-fill` × `(int)$s['rating']` then `bi-star` for the remainder up to 5; neutral "—" when `rating` is null per D-06), comment (`e()`), `orders_count` (D-07), and an actions cell with an edit link (`/suppliers/{id}/edit`, `bi-pencil`) and a delete form (`<form method="post" action="/suppliers/{id}/delete" data-confirm="Supprimer « {name} » ? Les commandes liées conserveront leur nom de fournisseur." class="d-inline">` containing `\App\Core\Csrf::field()` and a `bi-trash` submit). All dynamic output escaped with `e()`.
    Create `src/Views/suppliers/form.php` with `<?php /** @var array|null $supplier @var array $errors @var array $old */` docblock and the `$isEdit`/`$action`/`$val` helper (action `/suppliers/{id}` when edit else `/suppliers`). Form `method="post"` with `\App\Core\Csrf::field()`: a required `name` text input (is-invalid + invalid-feedback from `$errors['name']`), a `url` `type="url"` input, the star-rating widget (a hidden `<input name="rating" value="<?= e($val('rating')) ?>">` plus a row of `<button type="button" class="star-btn"><i class="bi bi-star"></i></button>` markup that `initStarRating()` will wire — include a small "effacer"/clear affordance so rating can return to unrated), and a `comment` `<textarea name="comment" rows="3">`. Submit + cancel (`/suppliers`) buttons. Escape all `$val()` output with `e()`.
  </action>
  <verify>
    <automated>php -l src/Views/suppliers/index.php && php -l src/Views/suppliers/form.php</automated>
  </verify>
  <acceptance_criteria>
    - Both `php -l` checks report "No syntax errors detected".
    - Source: index.php contains a delete `<form method="post" action="/suppliers/` with a `data-confirm=` attribute and `\App\Core\Csrf::field()`.
    - Source: index.php renders the linked-orders count using `orders_count` and renders rating via `bi-star-fill`/`bi-star` with a null → "—" branch.
    - Source: index.php escapes every dynamic value with `e(` (no raw `<?= $s[` echoes of name/url/comment); clickable URL uses `target="_blank"` and `rel="noopener"`.
    - Source: form.php contains a hidden `name="rating"` input pre-filled from `$val('rating')`, a required `name` input bound to `$errors['name']` is-invalid feedback, a `type="url"` url input (no filter_var), and a `name="comment"` textarea.
    - Source: form.php `$action` resolves to `/suppliers/{id}` in edit mode and `/suppliers` in create mode.
  </acceptance_criteria>
  <done>Suppliers index table (with confirm-modal delete) and create/edit form (with star-widget hidden input) created, fully escaped, no show page.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add the reusable initStarRating() widget to app.js + minimal CSS</name>
  <read_first>
    - public/assets/js/app.js — read the file. Specifically how existing init functions are structured/registered (e.g. the `initOrderForm` / DOMContentLoaded wiring and the `syncCurrency()` `.d-none` toggle around lines 279-281 / 367-391) so the new widget follows the same registration idiom and runs only when its DOM is present
    - public/assets/css/style.css — read the top to match the existing variable/section style before appending star-widget rules
    - .planning/phases/08-suppliers-and-product-cleanup/08-PATTERNS.md §"suppliers/form.php" (star widget note) — recommended hidden-input + clickable bi-star approach, kept self-contained for Phase 9 reuse
  </read_first>
  <action>
    Add an `initStarRating()` function to app.js that: finds each star-widget container (e.g. by a data attribute or a known wrapper class around the `.star-btn` row and its sibling hidden `input[name="rating"]`); reads the hidden input's current value and paints stars (filled `bi-star-fill` up to value, empty `bi-star` beyond); on `click` of a star button sets the hidden input to that 1-5 value and repaints; provides a clear-to-unrated affordance that sets the hidden input to '' (server stores null); optionally paints a hover preview. Guard so it no-ops when no widget is present on the page (mirrors how `syncCurrency`/`initOrderForm` only run when their elements exist). Register it on DOMContentLoaded alongside the existing initializers. Keep it generic (not hard-coded to suppliers) so Phase 9 reuses it. Append any needed `.star-btn` styling (cursor, color for filled/empty state, focus-visible outline) to style.css matching the existing token/section style. Do NOT modify the order form or any SUP-02 code in this plan.
  </action>
  <verify>
    <automated>node --check public/assets/js/app.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check public/assets/js/app.js` exits 0 (valid JS).
    - Source: `grep -c "initStarRating" public/assets/js/app.js` >= 1 and the function is invoked from the DOMContentLoaded/init path.
    - Source: the widget reads/writes a hidden `input[name="rating"]` and has a code path that sets it to '' (clear-to-unrated).
    - Source: the function guards against missing DOM (no uncaught error when no star widget on the page) — e.g. an early return / empty-NodeList loop.
    - Source: `grep -c "syncSupplier\|o-supplier" public/assets/js/app.js` returns 0 (no SUP-02 toggle added here — that is Plan 05).
    - Source: style.css contains a `.star-btn` (or equivalent star-widget) rule block.
  </acceptance_criteria>
  <done>Reusable initStarRating() added to app.js with clear-to-unrated and DOM guard; star styling in style.css; no SUP-02 code touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored supplier data → rendered HTML | name/url/comment originate from user input; XSS sink |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-12 | Tampering/Repudiation | stored XSS via supplier name/url/comment | mitigate | views escape all dynamic output with `e()`; URL rendered with `e()` in href + `target="_blank" rel="noopener"` (V5) |
| T-08-13 | Tampering | CSRF on delete form in the list | mitigate | delete form embeds `\App\Core\Csrf::field()`; server validates in destroy() (Plan 03) |
| T-08-14 | Spoofing | reverse-tabnabbing on external supplier URL | mitigate | `rel="noopener"` on the clickable URL link |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (RESEARCH audit: N/A); JS/CSS are in-repo edits, no CDN/origin change (existing CSP already covers assets) |
</threat_model>

<verification>
- `php -l` on both views clean; `node --check public/assets/js/app.js` exits 0.
- All source assertions pass; no SUP-02 (`o-supplier`) code present.
- `vendor/bin/phpunit` green.
</verification>

<success_criteria>
- Suppliers list table + create/edit form render with escaped output and the confirm-modal delete; a reusable clearable star widget exists in app.js; no order-form code changed.
</success_criteria>

<output>
Create `.planning/phases/08-suppliers-and-product-cleanup/08-04-SUMMARY.md` when done
</output>
