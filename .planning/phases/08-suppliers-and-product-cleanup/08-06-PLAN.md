---
phase: 08-suppliers-and-product-cleanup
plan: 06
type: execute
wave: 5
depends_on: ["08-01", "08-02", "08-03", "08-04", "08-05"]
files_modified: []
autonomous: false
requirements: [SUP-01, SUP-02, OPS-06]
must_haves:
  truths:
    - "After php bin/migrate.php runs against Aiven, the suppliers table and orders.supplier_id column exist"
    - "On the live Vercel URL a user can create, edit, and delete a supplier, scoped to their account"
    - "An order saves with a selected supplier (supplier_id + name) and one with 'Autre' free text; a legacy order still shows its name"
    - "Deleting a product removes its cover + gallery objects from Cloudinary (or logs a failure without blocking)"
  artifacts: []
  key_links:
    - from: "operator CLI"
      to: "Aiven MySQL"
      via: "php bin/migrate.php (schema.sql then Schema::ensure)"
      pattern: "migrate"
---

<objective>
Apply the Phase 8 schema to Aiven and verify all three requirements end-to-end on the live Vercel URL. This mirrors the operator Wave-2 split used in Phases 2/3: autonomous code lands first (Plans 01-05), then a clearly-marked operator step runs the migration and confirms behavior in production.

Purpose: The schema additions never run per-request (DB-03), so the operator must re-run `bin/migrate.php` against Aiven. Because the additions are purely additive and backward-compatible, running the migration BEFORE the Vercel deploy gives a zero-downtime window (RESEARCH Pitfall 1 / assumption A4).
Output: Verified production state — no code changes in this plan.
Depends on all five code plans being merged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-suppliers-and-product-cleanup/08-RESEARCH.md
@.planning/phases/08-suppliers-and-product-cleanup/08-VALIDATION.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Operator runs bin/migrate.php against Aiven (BEFORE deploying new code)</name>
  <what-built>
    Plan 01 added a `suppliers` table to sql/schema.sql + Schema::ensure() and a guarded `orders.supplier_id` FK (ON DELETE SET NULL) in Schema::ensure(). DDL never runs per-request (DB-03), so it must be applied via the one-shot CLI migration against the Aiven production database — the same proven step from Phases 2 and 3.
  </what-built>
  <how-to-verify>
    1. Ensure the Aiven CA cert is present at certs/aiven-ca.pem and DB_* env vars are set locally (same setup used for the Phase 2/3 migrations).
    2. Run, from the project root: `php bin/migrate.php`
    3. Confirm it exits cleanly with no SQL errors.
    4. Confirm the schema in Aiven: the `suppliers` table exists, and `orders` has a nullable `supplier_id` column with the `fk_orders_supplier` constraint (e.g. `SHOW COLUMNS FROM orders LIKE 'supplier_id'` returns one row; `SHOW CREATE TABLE orders` shows the FK with `ON DELETE SET NULL`).
    5. Re-run `php bin/migrate.php` a second time — it MUST still exit cleanly (idempotent: no "Duplicate column" / "table exists" error), proving the SHOW COLUMNS guard works.
    6. Recommended ordering: run this migration BEFORE pushing the new code to Vercel (additive schema is back-compat with the live v1.0 code → zero-downtime window). Then deploy.
  </how-to-verify>
  <read_first>
    - bin/migrate.php — the operator migration flow (runs sql/schema.sql statements then Schema::ensure(); CLI-gated)
    - .planning/phases/08-suppliers-and-product-cleanup/08-RESEARCH.md §"Common Pitfalls" Pitfall 1 (deploy/migrate ordering) and §"Environment Availability"
  </read_first>
  <acceptance_criteria>
    - `php bin/migrate.php` exits 0 with no SQL error on the first run.
    - `SHOW COLUMNS FROM orders LIKE 'supplier_id'` against Aiven returns exactly one row (nullable, INT UNSIGNED).
    - `SHOW CREATE TABLE orders` shows `fk_orders_supplier ... REFERENCES suppliers (id) ON DELETE SET NULL`.
    - The `suppliers` table exists with columns name/url/rating/comment + `fk_suppliers_user`.
    - A second `php bin/migrate.php` run also exits 0 (idempotent — no Duplicate column / table-exists error).
  </acceptance_criteria>
  <resume-signal>Type "migrated" once the migration ran cleanly and the suppliers table + orders.supplier_id column are confirmed in Aiven (and re-run is idempotent), or describe the error.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify SUP-01, SUP-02, OPS-06 on the live Vercel URL</name>
  <what-built>
    Plans 03-05 added supplier CRUD (Fournisseurs tab), the order↔supplier dropdown with "Autre" fallback and id+name dual-write, and Plan 02 added best-effort Cloudinary purge on product delete. The schema is now live (Task 1). Deploy the new code if not already deployed, then verify behavior in production.
  </what-built>
  <how-to-verify>
    Ensure the new code is deployed to Vercel (push if needed), then on the public alias (resseltrack-nu.vercel.app) logged in as a test account:
    SUP-01 (CRUD + scoping):
      1. Click the new "Fournisseurs" nav entry (Activité section, desktop sidebar + mobile offcanvas).
      2. Create a supplier with a name, a URL, a 1-5 star rating (set via clickable stars), and a comment → it appears in the table with stars, the clickable URL (opens in a new tab), the comment, and a linked-orders count of 0.
      3. Edit it (change rating + clear it to unrated → neutral "—" shows) and save.
      4. Confirm validation: submitting with an empty name shows the "Le nom du fournisseur est requis." error.
      5. (If a second account is available) confirm account B cannot see or edit account A's supplier (/suppliers/{A-id}/edit redirects with "Fournisseur introuvable.").
    SUP-02 (link + backward compat):
      6. Create an order selecting the saved supplier → after save, the order stores supplier_id and shows the supplier name; the linked-orders count for that supplier increments to 1.
      7. Create an order choosing "Autre" and typing a free-text supplier → it saves the free text, supplier_id null.
      8. Save an order with NO supplier selected → accepted.
      9. Open a legacy/pre-existing order (no supplier_id) → it still displays its original free-text supplier name unchanged.
      10. Delete the supplier linked in step 6 → the order keeps its free-text supplier name and the link becomes null (ON DELETE SET NULL); no order is deleted.
    OPS-06 (Cloudinary purge):
      11. Create a product with a cover + at least one gallery image (Cloudinary URLs), then delete the product → confirm via the Cloudinary console (or a HEAD request returning 404) that the cover + gallery objects are gone, and the product rows are removed from the DB. If a purge call fails, confirm the product is still deleted and the failure is in the function logs (error_log).
  </how-to-verify>
  <read_first>
    - .planning/phases/08-suppliers-and-product-cleanup/08-VALIDATION.md §"Manual-Only Verifications"
    - .planning/ROADMAP.md §"Phase 8" Success Criteria
  </read_first>
  <acceptance_criteria>
    - SUP-01: supplier create/edit/delete work on the live URL; rating stars set + clear-to-unrated work; empty-name validation fires; cross-account access is blocked (if a second account is tested).
    - SUP-02: an order saves with a selected supplier (supplier_id + name) AND one with "Autre" free text; an order saves with no supplier; a legacy order still renders its name; deleting a linked supplier unlinks (supplier_id null) without deleting the order.
    - OPS-06: deleting a product with cover + gallery removes those objects from Cloudinary (HEAD 404 / console) and always removes the DB rows; a purge failure is logged, not blocking.
    - No "Base table or view not found: suppliers" or "Unknown column 'supplier_id'" errors in Vercel function logs.
  </acceptance_criteria>
  <resume-signal>Type "verified" once all three requirements pass on the live URL, or list any failures (they become gap-closure input).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator CLI → Aiven MySQL | DDL applied here only (no per-request DDL) |
| public Vercel URL → app | production verification surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-20 | Denial of Service | deploy-before-migrate 500 window | mitigate | migrate Aiven BEFORE the Vercel deploy (additive/back-compat schema → zero-downtime); verify suppliers table + supplier_id before deploy |
| T-08-21 | Tampering | non-idempotent re-run corrupting orders table | mitigate | SHOW COLUMNS guard (Plan 01) verified by the required second idempotent migration run |
| T-08-22 | Information Disclosure | cross-account supplier visibility in prod | mitigate | live cross-account check in Task 2 step 5 confirms `:uid` scoping + find() ownership |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (RESEARCH audit: N/A) |
</threat_model>

<verification>
- Migration runs cleanly and idempotently against Aiven; suppliers table + orders.supplier_id FK confirmed.
- All SUP-01 / SUP-02 / OPS-06 acceptance criteria pass on the live Vercel URL.
- No suppliers/supplier_id SQL errors in function logs.
</verification>

<success_criteria>
- Phase 8 schema applied to production and all three requirements verified end-to-end on the live URL.
</success_criteria>

<output>
Create `.planning/phases/08-suppliers-and-product-cleanup/08-06-SUMMARY.md` when done
</output>
