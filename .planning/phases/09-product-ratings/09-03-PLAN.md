---
phase: 09-product-ratings
plan: 03
type: execute
wave: 2
depends_on: ["09-01", "09-02"]
files_modified:
  - src/Views/products/form.php
  - src/Views/products/index.php
autonomous: true
requirements: [RATE-01]

must_haves:
  truths:
    - "The product edit form has a 'Note du produit' section: a clickable 1-5 star widget (hidden input name=rating) + a 'Commentaire' textarea (name=rating_note), saved alongside all other fields via the existing update()/store() (D-02 entry point 1)"
    - "The rating section reuses the existing initStarRating() widget verbatim (auto-wired on [data-star-rating]) — NO new JS, NO new widget engine"
    - "The product list shows a star badge next to the product NAME for rated products; unrated products show nothing (neutral); the comment is NOT shown in the list (D-04)"
    - "rating_note is escaped with e() on output (D-06); the comment field name is rating_note, distinct from the existing name=description"
  artifacts:
    - path: "src/Views/products/form.php"
      provides: "Note section with star widget (name=rating) + comment textarea (name=rating_note)"
      contains: "name=\"rating_note\""
    - path: "src/Views/products/index.php"
      provides: "read-only star badge next to the product name, gated on rating !== null"
      contains: "supplier-stars"
  key_links:
    - from: "src/Views/products/form.php"
      to: "initStarRating() in app.js"
      via: "data-star-rating container + input[type=hidden][name=rating]"
      pattern: "data-star-rating"
    - from: "src/Views/products/form.php name=rating / name=rating_note"
      to: "ProductController::validate()"
      via: "POST fields parsed in validate()"
      pattern: "name=\"rating\""
---

<objective>
Wire the two display/edit surfaces that reuse Phase 8 widgets unchanged: add a "Note du produit" section to `products/form.php` (clickable star widget + comment textarea, a drop-in copy of `suppliers/form.php`) and a read-only star badge next to the product name in `products/index.php` (mirroring `suppliers/index.php` read-only stars). No JS and no CSS changes — `initStarRating()` auto-wires the form widget and `.supplier-stars` / `.star-*` classes are reused verbatim.

Purpose: Deliver D-02 entry point 1 (form rating) and D-04 (list badge next to name, no column, comment hidden).
Output: Two modified view files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-product-ratings/09-CONTEXT.md
@.planning/phases/09-product-ratings/09-PATTERNS.md
@src/Views/products/form.php
@src/Views/products/index.php
@src/Views/suppliers/form.php
@src/Views/suppliers/index.php

<interfaces>
<!-- Verified analogs (09-PATTERNS.md): -->
<!-- suppliers/form.php:32-47 — star-widget block (data-star-rating + input[type=hidden][name=rating] + 5 .star-btn + .star-clear[data-star-clear]) + comment textarea. Drop-in copy. -->
<!-- products/form.php:5 $val() helper (identical to suppliers'); existing market-price section header at :52; existing name="description" at :37 (DO NOT reuse — comment field must be name="rating_note"). -->
<!-- suppliers/index.php:46-55 — read-only stars span gated on rating, NULL placeholder. -->
<!-- products/index.php — card grid (NOT a table); name link at :134, badge row :135-146. Reuses .supplier-stars (style.css:984-985) verbatim. $p['rating'] present on every row (searchForUser → SELECT *). -->
<!-- initStarRating() (app.js:575-623) auto-targets every [data-star-rating] on DOMContentLoaded — the form widget needs ZERO new JS. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the "Note du produit" section to products/form.php</name>
  <files>src/Views/products/form.php</files>
  <read_first>
    - src/Views/products/form.php ($val() at :5, the market-price section header at :52, the closing of the price section ~:90-91, existing name="description" at :37)
    - src/Views/suppliers/form.php (:32-47 the star-widget + comment block to copy verbatim)
    - .planning/phases/09-product-ratings/09-PATTERNS.md (section "src/Views/products/form.php")
  </read_first>
  <action>
    Insert a new `form-section-title` block titled "Note du produit" after the market-price section (after ~:90, before the section's closing wrapper), mirroring the existing "Prix du marché" header at :52. Copy the star-widget markup verbatim from suppliers/form.php:34-42: a `<div class="star-rating" data-star-rating>` containing `<input type="hidden" name="rating" value="<?= e($val('rating')) ?>">`, five `<button type="button" class="star-btn" data-value="$i">` star buttons, and a `<button ... data-star-clear>Effacer</button>`. Add the `$errors['rating']` inline error display and a French help text. Below it add a "Commentaire" `<textarea name="rating_note" class="form-control" rows="3"><?= e($val('rating_note')) ?></textarea>`. The comment field name MUST be `rating_note` (NOT `comment`, NOT `description` — name collision check). Use the existing `e()` and `$val()` helpers; do not add any JS (initStarRating auto-wires data-star-rating).
  </action>
  <acceptance_criteria>
    - `grep -c 'name="rating"' src/Views/products/form.php` returns 1 (the hidden star input)
    - `grep -c 'name="rating_note"' src/Views/products/form.php` returns 1 (the comment textarea)
    - `grep -c "data-star-rating" src/Views/products/form.php` returns 1
    - `grep -c "Note du produit" src/Views/products/form.php` returns 1
    - The hidden rating input value and the textarea both render through `e($val(...))` (escaped); `grep -c "e(\$val('rating_note'))" src/Views/products/form.php` returns 1
    - `php -l src/Views/products/form.php` reports "No syntax errors detected"
    - No `name="comment"` and no second `name="description"` was introduced
  </acceptance_criteria>
  <verify>
    <automated>php -l src/Views/products/form.php</automated>
  </verify>
  <done>Form has a Note section: star widget (name=rating, auto-wired) + comment textarea (name=rating_note), pre-filled in edit mode, escaped.</done>
</task>

<task type="auto">
  <name>Task 2: Add a read-only rating badge next to the product name in products/index.php</name>
  <files>src/Views/products/index.php</files>
  <read_first>
    - src/Views/products/index.php (the name link at :134 and the identity/badge block :135-146; confirm $p / $r['product'] carries rating from SELECT *)
    - src/Views/suppliers/index.php (:46-55 read-only stars with NULL placeholder)
    - .planning/phases/09-product-ratings/09-PATTERNS.md (section "src/Views/products/index.php" — D-04: badge near name, no column, comment hidden)
  </read_first>
  <action>
    Right after the product-name link (`:134`), add a small read-only stars span gated on `$p['rating'] !== null`: cast `$rating = (int) $p['rating']`, render a `<span class="supplier-stars" title="$rating/5" aria-label="$rating sur 5" style="font-size:.8rem">` containing five `<i class="bi bi-star-fill | bi-star">` icons (filled for `$i <= $rating`). When rating is null, render nothing (neutral placeholder, D-04). Reuse the existing `.supplier-stars` CSS class verbatim — NO new CSS. Do NOT render `rating_note` here (comment is detail-only, D-04). Do NOT add a new table column header (this is a card grid). Escape numeric output via `(int)` cast.
  </action>
  <acceptance_criteria>
    - `grep -c "supplier-stars" src/Views/products/index.php` returns 1
    - `grep -c "\$p\['rating'\] !== null" src/Views/products/index.php` returns 1 (badge gated on rated)
    - The badge sits immediately after the product-name link (same identity block)
    - `grep -c "rating_note" src/Views/products/index.php` returns 0 (comment NOT shown in list, D-04)
    - `php -l src/Views/products/index.php` reports "No syntax errors detected"
  </acceptance_criteria>
  <verify>
    <automated>php -l src/Views/products/index.php</automated>
  </verify>
  <done>Rated products show a star badge next to the name; unrated show nothing; layout intact for both; no comment in the list.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server → rendered HTML | stored rating_note / rating values reach the browser; must be escaped/cast to avoid stored XSS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | Tampering (stored XSS) | rating_note in the form textarea, rating in the badge | mitigate | rating_note rendered via `e($val('rating_note'))`; rating rendered via `(int)` cast — no raw user text reaches the DOM |
| T-09-05 | Spoofing (CSRF) | the product edit form | mitigate | the form already carries `Csrf::field()` (existing product form); the new Note section adds only fields inside that same form, no new endpoint |
| T-09-SC | Tampering | composer/npm installs | accept | no dependency changes in this phase — no install tasks |
</threat_model>

<verification>
- `php -l` clean on both views.
- Live: editing a product, setting 1-5 stars + comment, saving → list badge appears next to the name and the comment is stored (verified by the operator in Plan 09-05). Unrated products render with no badge and no layout break.
</verification>

<success_criteria>
- Form has a reusable star widget (name=rating) + comment textarea (name=rating_note), no new JS/CSS.
- List shows a star badge next to the name for rated products only; comment not shown.
</success_criteria>

<output>
Create `.planning/phases/09-product-ratings/09-03-SUMMARY.md` when done.
</output>
