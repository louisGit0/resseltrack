---
phase: 09-product-ratings
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sql/schema.sql
  - src/Core/Schema.php
autonomous: true
requirements: [RATE-01]

must_haves:
  truths:
    - "products gains rating (TINYINT UNSIGNED NULL) + rating_note (TEXT NULL), additive and backward-compatible — existing products keep rating = NULL, zero data migration (D-06)"
    - "Re-running php bin/migrate.php is idempotent — second run exits 0 with no Duplicate column error (SHOW COLUMNS guard)"
    - "A fresh install from sql/schema.sql creates both columns inside the products block"
  artifacts:
    - path: "src/Core/Schema.php"
      provides: "SHOW COLUMNS-guarded ALTER TABLE products adding rating + rating_note"
      contains: "SHOW COLUMNS FROM products LIKE 'rating'"
    - path: "sql/schema.sql"
      provides: "rating + rating_note columns in the products CREATE TABLE block"
      contains: "rating_note TEXT NULL"
  key_links:
    - from: "src/Core/Schema.php"
      to: "products.rating / products.rating_note"
      via: "guarded ALTER TABLE in Schema::ensure()"
      pattern: "ADD COLUMN rating TINYINT UNSIGNED NULL"
---

<objective>
Add the two additive nullable columns RATE-01 needs — `products.rating` (TINYINT UNSIGNED NULL) and `products.rating_note` (TEXT NULL) — to both the fresh-install DDL (`sql/schema.sql`) and the idempotent live migration (`src/Core/Schema.php`). No FK, no index; mirrors the Phase 8 `orders.supplier_id` / `market_price_*` additive-column pattern exactly.

Purpose: Provide the storage RATE-01 depends on, applied idempotently with zero data migration so existing products keep `rating = NULL`.
Output: Two modified files; the live columns are added when the operator re-runs `php bin/migrate.php` (Plan 09-05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-product-ratings/09-CONTEXT.md
@.planning/phases/09-product-ratings/09-PATTERNS.md
@.planning/phases/08-suppliers-and-product-cleanup/08-06-SUMMARY.md

<interfaces>
<!-- Analog precedent already in these files (verified this session, 09-PATTERNS.md). -->
<!-- Schema.php:43-50 — market_price guarded ALTER (SHOW COLUMNS guard, multi-column nullable ALTER, no FK). -->
<!-- Schema.php:106-107 — suppliers.rating TINYINT UNSIGNED NULL + suppliers.comment TEXT NULL (the exact column types to mirror). -->
<!-- schema.sql:28-43 — products CREATE TABLE block; :35-36 market_price_* nullable columns with ;-free inline comments. -->
<!-- CRITICAL deviation (08-06-SUMMARY): bin/migrate.php splits on ';' (explode). A ';' inside a schema.sql comment breaks the splitter. New comments MUST be ';'-free. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add rating + rating_note to the products block in sql/schema.sql</name>
  <files>sql/schema.sql</files>
  <read_first>
    - sql/schema.sql (the products CREATE TABLE block, :28-43; the market_price_* nullable columns at :35-36 are the precedent)
    - .planning/phases/09-product-ratings/09-PATTERNS.md (section "sql/schema.sql" — the ;-free comment rule)
    - .planning/phases/08-suppliers-and-product-cleanup/08-06-SUMMARY.md (the ; -in-comment deviation that broke the first Phase 8 migrate run)
  </read_first>
  <action>
    Inside the existing products CREATE TABLE block, after the `market_price_used` column line and before `created_at`, add two columns: `rating TINYINT UNSIGNED NULL` and `rating_note TEXT NULL`. Give each a short French inline comment in the exact style of the `-- prix constaté…` comment already on `market_price_used`. The comments MUST contain no `;` character — `bin/migrate.php` splits statements on `;` and a semicolon inside a comment produced the Phase 8 first-run failure (commit 6c8dcd7). Do not change column order of existing columns; do not touch any other table.
  </action>
  <acceptance_criteria>
    - `grep -c "rating_note TEXT NULL" sql/schema.sql` returns 1
    - `grep -c "rating      TINYINT UNSIGNED NULL" sql/schema.sql` returns at least 1 (rating column present in the products block)
    - No `;` appears in either new comment: the two added lines, when filtered for their comment portion, contain no `;` (the only `;`-free guarantee that matters for the splitter)
    - The two new column lines sit between `market_price_used` and `created_at` inside the products block
  </acceptance_criteria>
  <verify>
    <automated>php -l sql/schema.sql 2>/dev/null; grep -c "rating_note TEXT NULL" sql/schema.sql</automated>
  </verify>
  <done>products block declares rating + rating_note as nullable columns with ;-free comments; fresh installs get both columns.</done>
</task>

<task type="auto">
  <name>Task 2: Add a SHOW COLUMNS-guarded ALTER for rating + rating_note in Schema::ensure()</name>
  <files>src/Core/Schema.php</files>
  <read_first>
    - src/Core/Schema.php (the market_price guarded ALTER at :43-50 — the shape to copy; the suppliers.rating/comment column types at :106-107)
    - .planning/phases/09-product-ratings/09-PATTERNS.md (section "src/Core/Schema.php" — guard on the single sentinel column `rating`)
  </read_first>
  <action>
    In `Schema::ensure()`, immediately after the existing `market_price` guarded block (around :50), add an analogous block: query `SHOW COLUMNS FROM products LIKE 'rating'`; if it returns no row, run a single `ALTER TABLE products` that ADDs both `rating TINYINT UNSIGNED NULL AFTER market_price_used` and `rating_note TEXT NULL AFTER rating`. Guard on the single sentinel column `rating` (one SHOW COLUMNS covers the pair, exactly like the market_price guard covers both market columns). Column types mirror `suppliers.rating` / `suppliers.comment` (:106-107). No FK, no index. Keep `declare(strict_types=1)` and the file's existing structure intact.
  </action>
  <acceptance_criteria>
    - `grep -c "SHOW COLUMNS FROM products LIKE 'rating'" src/Core/Schema.php` returns 1
    - `grep -c "ADD COLUMN rating TINYINT UNSIGNED NULL" src/Core/Schema.php` returns 1
    - `grep -c "ADD COLUMN rating_note TEXT NULL" src/Core/Schema.php` returns 1
    - `php -l src/Core/Schema.php` reports "No syntax errors detected"
    - The guard is an `if (!$has...)` block (no unconditional ALTER that would throw Duplicate column on the 2nd run)
  </acceptance_criteria>
  <verify>
    <automated>php -l src/Core/Schema.php</automated>
  </verify>
  <done>Schema::ensure() adds both columns once, guarded; a second migrate run is a clean no-op (idempotent, like supplier_id in 08-06).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator CLI → Aiven DDL | bin/migrate.php applies schema changes; runs only as the operator, never per-request (DDL is out of the request path per DB-03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-DDL | Denial of Service | second migrate.php run | mitigate | SHOW COLUMNS guard makes the ALTER idempotent — no Duplicate column abort on re-run (proven for supplier_id in 08-06) |
| T-09-SPL | Tampering | bin/migrate.php ';' splitter | mitigate | new schema.sql comments are ';'-free, avoiding the Phase 8 statement-fragment corruption |
| T-09-SC | Tampering | npm/pip/composer installs | accept | no dependency changes in this phase — no install tasks, no Package Legitimacy Audit required |
</threat_model>

<verification>
- `php -l sql/schema.sql` and `php -l src/Core/Schema.php` clean.
- `vendor/bin/phpunit` stays green (ProfitCalculator regression guard).
- Live idempotency is proven by the operator in Plan 09-05 (`php bin/migrate.php` x2 → exit 0 both; `SHOW COLUMNS FROM products LIKE 'rating'` returns one row).
</verification>

<success_criteria>
- products.rating + products.rating_note declared in both schema.sql (fresh) and Schema::ensure() (live), nullable, additive.
- The migration guard is idempotent; no `;` in new schema.sql comments.
</success_criteria>

<output>
Create `.planning/phases/09-product-ratings/09-01-SUMMARY.md` when done.
</output>
