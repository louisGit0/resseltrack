---
phase: 09-product-ratings
plan: 05
type: execute
wave: 3
depends_on: ["09-01", "09-02", "09-03", "09-04"]
files_modified: []
autonomous: false
requirements: [RATE-01]

must_haves:
  truths:
    - "php bin/migrate.php runs against Aiven and adds products.rating + products.rating_note; a second run is idempotent (exit 0, no Duplicate column) — migration runs BEFORE the Vercel deploy for zero-downtime (additive, backward-compatible)"
    - "RATE-01 verified end-to-end on the live URL: form save (rating + comment), one-click quick-rate (D-03 route, ownership + CSRF), list badge near name (D-04), detail stars + comment (D-05)"
    - "Existing products are unaffected — rating NULL by default, no data migration, no layout break (D-01/D-06)"
  artifacts:
    - path: ".planning/phases/09-product-ratings/09-05-SUMMARY.md"
      provides: "operator migration log + live verification evidence for RATE-01"
  key_links:
    - from: "operator CLI"
      to: "Aiven products table"
      via: "php bin/migrate.php (Schema::ensure() guarded ALTER)"
      pattern: "SHOW COLUMNS FROM products LIKE 'rating'"
---

<objective>
Apply the additive schema to the live Aiven database via `php bin/migrate.php` (operator step, run BEFORE the Vercel deploy for zero-downtime — the columns are additive and backward-compatible), then verify RATE-01 end-to-end on the deployed Vercel URL. This mirrors the Phase 8 08-06 operator+verification plan exactly.

Purpose: Turn the merged code (Plans 09-01..09-04) into a live, verified RATE-01 feature with the production database left pristine.
Output: `09-05-SUMMARY.md` recording the migration result and the live verification evidence; RATE-01 marked complete in REQUIREMENTS.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-product-ratings/09-CONTEXT.md
@.planning/phases/09-product-ratings/09-VALIDATION.md
@.planning/phases/08-suppliers-and-product-cleanup/08-06-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Operator — apply the migration to Aiven (run before deploy)</name>
  <what-built>
    Plans 09-01..09-04 added products.rating + products.rating_note (schema.sql + Schema::ensure()), the model/controller persistence + rate() action, the form/list views, and the detail quick-rate. The columns are additive and backward-compatible.
  </what-built>
  <read_first>
    - .planning/phases/08-suppliers-and-product-cleanup/08-06-SUMMARY.md (the exact Phase 8 operator-migration procedure + the ;-in-comment deviation that broke the first run)
    - .planning/phases/09-product-ratings/09-VALIDATION.md (Idempotent migration row)
  </read_first>
  <how-to-verify>
    1. Ensure the .env / environment points at the production Aiven MySQL (host/port, defaultdb, avnadmin, TLS cert at certs/aiven-ca.pem).
    2. Run `php bin/migrate.php` — confirm exit 0 (schema.sql applied + Schema::ensure() clean, no statement-fragment error from a ';' in a comment).
    3. Run `php bin/migrate.php` a SECOND time — confirm exit 0 again (idempotent; SHOW COLUMNS guard → no "Duplicate column" error).
    4. Confirm on Aiven: `SHOW COLUMNS FROM products LIKE 'rating'` returns one row (TINYINT UNSIGNED, nullable) and `SHOW COLUMNS FROM products LIKE 'rating_note'` returns one row (TEXT, nullable).
    5. Migration is additive → run it BEFORE pushing the deploy to Vercel (zero-downtime, per Phase 8 / RESEARCH Pitfall 1).
  </how-to-verify>
  <acceptance_criteria>
    - `php bin/migrate.php` exits 0 on both the first and second run (idempotent — no Duplicate column / table-exists error)
    - `SHOW COLUMNS FROM products LIKE 'rating'` returns exactly one row; type TINYINT UNSIGNED, Null=YES
    - `SHOW COLUMNS FROM products LIKE 'rating_note'` returns exactly one row; type TEXT, Null=YES
    - Existing product rows have rating = NULL (no data migration occurred)
  </acceptance_criteria>
  <resume-signal>Type "migrated" once both runs exit 0 and both columns are confirmed on Aiven, or paste the error.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator — verify RATE-01 end-to-end on the live Vercel URL</name>
  <what-built>
    The full RATE-01 feature is deployed: edit-form rating section, list badge, detail-page interactive quick-rate, and the POST /products/{id}/rate route with CSRF + ownership.
  </what-built>
  <read_first>
    - .planning/phases/09-product-ratings/09-VALIDATION.md (Manual-Only Verifications table)
    - .planning/phases/09-product-ratings/09-CONTEXT.md (D-01..D-06 — the behaviors to confirm)
  </read_first>
  <how-to-verify>
    On the production URL (public alias resseltrack-nu.vercel.app), logged in:
    1. Smoke: open a product detail page and the product list — no 500, no "Base table/Unknown column/SQLSTATE" error; existing (unrated) products render with a neutral placeholder and no broken layout (D-01/D-06 backward-compat).
    2. Form save (D-02 entry 1, D-06): edit a product, set a 1-5 star rating + a "Commentaire", save → list shows the star badge next to the name (D-04), detail shows the stars + the full comment below (D-05). Confirm the comment is NOT shown in the list (D-04).
    3. Quick-rate (D-02 entry 2, D-03): on a product detail page, click a star → reload → the rating persisted; click "Effacer" → rating cleared to unrated. Confirm a forged/absent CSRF token is rejected and that a product you do not own cannot be rated (ownership/IDOR — 302 back, no change).
    4. Validation (D-06): an out-of-range rating is rejected with "La note doit être comprise entre 1 et 5." (server-side).
    5. Leave the production database pristine — remove any throwaway verification data.
  </how-to-verify>
  <acceptance_criteria>
    - Form save persists rating + rating_note; list badge near the name; detail shows stars + escaped comment; comment absent from the list
    - One-click quick-rate persists across reload; Effacer clears to NULL; CSRF + ownership enforced on POST /products/{id}/rate
    - Unrated/existing products render with a neutral placeholder, no layout break, rating still NULL
    - `vendor/bin/phpunit` is green (ProfitCalculator + existing flows regression)
    - RATE-01 marked complete in .planning/REQUIREMENTS.md
  </acceptance_criteria>
  <resume-signal>Type "approved" once RATE-01 is verified live, or describe the issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator CLI → Aiven DDL | bin/migrate.php applies additive columns; operator-only, never per-request |
| public internet → live RATE-01 endpoints | the quick-rate POST and form save are exercised against the production app; CSRF + ownership must hold |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-DDL | Denial of Service | second migrate.php run | mitigate | idempotent guard verified by running migrate.php twice (Task 1) |
| T-09-01 | Elevation of Privilege (IDOR) | live POST /products/{id}/rate | mitigate | verified live that a non-owned product cannot be rated (Task 2 step 3) |
| T-09-02 | Spoofing (CSRF) | live quick-rate / form | mitigate | verified live that forged/absent CSRF is rejected (Task 2 step 3) |
| T-09-07 | Information Disclosure | verification data left in prod | mitigate | throwaway verification data removed; production DB left pristine (Task 2 step 5), same as 08-06 |
| T-09-SC | Tampering | composer/npm installs | accept | no dependency changes in this phase — no install tasks |
</threat_model>

<verification>
- migrate.php idempotent (exit 0 x2) + both columns present on Aiven (Task 1).
- RATE-01 form + quick-rate + list + detail + validation + backward-compat all confirmed on the live URL; phpunit green; REQUIREMENTS.md updated (Task 2).
</verification>

<success_criteria>
- products.rating + products.rating_note live on Aiven; migration idempotent; deploy zero-downtime.
- RATE-01 verified end-to-end in production; production database pristine; RATE-01 marked complete.
</success_criteria>

<output>
Create `.planning/phases/09-product-ratings/09-05-SUMMARY.md` when done.
</output>
