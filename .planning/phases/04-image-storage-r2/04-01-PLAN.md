---
phase: 04-image-storage-r2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - composer.json
  - api/php.ini
  - .env.example
  - public/index.php
  - src/Services/R2Storage.php
  - src/Controllers/ProductController.php
autonomous: true
requirements: [STORE-01, STORE-02, STORE-03, STORE-04, STORE-05]

must_haves:
  truths:
    - "composer.json require block contains aws/aws-sdk-php ^3 (D-01) — the first runtime third-party Composer dependency"
    - "composer.json adds aws/aws-sdk-php ^3 to require and a scripts.vercel = @composer dump-autoload --optimize entry; the FULL SDK ships in the Lambda (~37.5 MB, within the 250 MB limit) — no pre-autoload-dump removeUnusedServices slimming is relied upon, because vercel-php runs composer install with --no-scripts so that hook never fires during the Vercel build (RESEARCH Open Question #1 / Pitfall 2)"
    - "public/index.php requires vendor/autoload.php guarded by is_file() BEFORE the maison spl_autoload_register (D-03)"
    - "public/index.php CSP img-src directive includes https://*.r2.dev in addition to 'self' data: so r2.dev images are not blocked (orchestrator CSP override; pre-satisfies part of Phase 5 SEC-03 for STORE-02)"
    - "src/Services/R2Storage.php is a final class with declare(strict_types=1) in namespace App\\Services wrapping Aws\\S3\\S3Client with region 'auto', endpoint https://<R2_ACCOUNT_ID>.r2.cloudflarestorage.com, explicit Credentials, NO use_path_style_endpoint, NO ACL (D-04, RESEARCH Pattern 1)"
    - "R2Storage::put(string,string,string): string returns R2_PUBLIC_BASE_URL + '/' + key; R2Storage::delete(string): void derives the key by stripping R2_PUBLIC_BASE_URL (D-04, D-05)"
    - "R2Storage reads all config via Env::get() only (R2_ACCOUNT_ID/R2_ACCESS_KEY_ID/R2_SECRET_ACCESS_KEY/R2_BUCKET/R2_PUBLIC_BASE_URL) — no Models/Core deps beyond Env (Services convention)"
    - "ProductController::storeUploadedFile() calls (new R2Storage())->put() instead of move_uploaded_file() and returns the full R2 URL in the same [?string,?string] tuple (D-06)"
    - "ProductController::deleteImage() FULLY replaces the dirname()/is_file()/@unlink() local-file block with (new R2Storage())->delete() wrapped in try/catch + error_log (D-06, D-08, RESEARCH Pitfall 4)"
    - "ProductController::MAX_UPLOAD_BYTES = (int)(3.5 * 1024 * 1024) with a clear French over-limit message (~3,5 Mo) (D-07, STORE-05)"
    - "api/php.ini sets upload_max_filesize = 5M and post_max_size = 5M so PHP does not silently empty $_FILES before the app-level guard fires (D-07, RESEARCH Pitfall 5)"
    - ".env.example documents the 5 R2_* variables with no secret values (D-09)"
    - "/vendor stays in .vercelignore unchanged (D-02 — vercel-php rebuilds vendor in the Lambda; source upload exclusion does not break it)"
  artifacts:
    - path: "src/Services/R2Storage.php"
      provides: "Pure-I/O R2 storage service wrapping Aws\\S3\\S3Client (put/delete/deriveKey)"
      contains: "final class R2Storage"
      min_lines: 45
    - path: "composer.json"
      provides: "aws/aws-sdk-php ^3 runtime dependency + Vercel slimming scripts"
      contains: "aws/aws-sdk-php"
    - path: "public/index.php"
      provides: "guarded vendor/autoload.php require + CSP img-src r2.dev"
      contains: "vendor/autoload.php"
    - path: "src/Controllers/ProductController.php"
      provides: "upload→R2::put + delete→R2::delete swap + 3.5MB guard"
      contains: "R2Storage"
    - path: "api/php.ini"
      provides: "upload_max_filesize + post_max_size = 5M"
      contains: "upload_max_filesize"
    - path: ".env.example"
      provides: "R2_* variable documentation (no secrets)"
      contains: "R2_ACCOUNT_ID"
  key_links:
    - from: "public/index.php"
      to: "vendor/autoload.php"
      via: "is_file()-guarded require before spl_autoload_register"
      pattern: "is_file.*vendor/autoload"
    - from: "src/Controllers/ProductController.php"
      to: "App\\Services\\R2Storage"
      via: "new R2Storage()->put / ->delete inside storeUploadedFile/deleteImage"
      pattern: "R2Storage"
    - from: "src/Services/R2Storage.php"
      to: "Aws\\S3\\S3Client"
      via: "putObject / deleteObject against the R2 endpoint"
      pattern: "S3Client"
    - from: "public/index.php CSP img-src"
      to: "https://*.r2.dev"
      via: "Content-Security-Policy header allowing r2.dev image origin"
      pattern: "r2\\.dev"
---

<objective>
Deliver the fully-autonomous code half of Phase 4: move product image upload and delete off the ephemeral serverless filesystem onto Cloudflare R2 via `aws/aws-sdk-php`. (1) Add `aws/aws-sdk-php ^3` to `composer.json` and the Vercel slimming scripts; raise the PHP upload limits in `api/php.ini`; document the 5 `R2_*` env vars in `.env.example` (D-01, D-07, D-09). (2) Wire Composer's `vendor/autoload.php` into `public/index.php` guarded by `is_file()` BEFORE the maison autoloader, and add `https://*.r2.dev` to the CSP `img-src` directive so uploaded images actually DISPLAY in production (D-03 + orchestrator CSP override). (3) Create `final class R2Storage` wrapping `Aws\S3\S3Client` (D-04, D-05) and swap `ProductController::storeUploadedFile()`/`deleteImage()` from disk I/O to R2, raising the size guard to 3.5 MB (D-06, D-07).

Purpose: This is the autonomous half — pure PHP + JSON + ini, all locally lintable. STORE-04 (existing local-path records) needs NO code: the production DB is empty (0 products, D-08), so the documented fallback is "no migration needed; local Docker seed images stay local-only." The CSP `img-src` change is pulled into this phase (not deferred to Phase 5) because STORE-02 — "images display in production from their r2.dev URL" — is literally false while the current `img-src 'self' data:` blocks r2.dev; this pre-satisfies part of Phase 5 SEC-03. Wave 2 (Plan 04-02) is the operator half: create the R2 bucket + token, set the 5 Vercel env vars, redeploy, and verify STORE-01..05 live.

Output: New `src/Services/R2Storage.php`; modified `composer.json`, `api/php.ini`, `.env.example`, `public/index.php`, `src/Controllers/ProductController.php`. `vendor/` is NOT committed and `/vendor` stays in `.vercelignore` (D-02). No view changes (D-05 — full URL stored, rendered as-is).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-image-storage-r2/04-CONTEXT.md
@.planning/phases/04-image-storage-r2/04-RESEARCH.md

<!-- Files this plan reads/modifies -->
@composer.json
@api/php.ini
@.env.example
@.vercelignore
@public/index.php
@src/Controllers/ProductController.php
@src/Services/ExchangeRateService.php
@src/Models/ProductImage.php

<interfaces>
<!-- Contracts already in the codebase — no exploration required. -->

App\Core\Env::get(string $key, ?string $default = null): ?string
  - Static config reader. Real env vars (Vercel) win over .env. R2Storage reads ALL its config through this — same pattern as Database/Session. Reference as `use App\Core\Env;` then `Env::get('R2_BUCKET')`.

App\Services\ExchangeRateService (src/Services/ExchangeRateService.php) — the Services convention analog
  - final class, declare(strict_types=1), namespace App\Services, no Core/Models deps, pure I/O. R2Storage mirrors this shape. ExchangeRateService is also instantiated LAZILY by its caller (new inside the method, not the constructor) — R2Storage must be instantiated the same way inside storeUploadedFile()/deleteImage() (D-06 anti-pattern: no eager construction in the controller constructor).

App\Controllers\ProductController (src/Controllers/ProductController.php) — the swap surface
  - storeUploadedFile(string $tmp, int $size): array (lines 465-488) — returns [?string $path, ?string $error]. Currently: size guard vs MAX_UPLOAD_BYTES, finfo MIME allowlist (jpeg/png/webp/gif), bin2hex(random_bytes(8)).ext naming, move_uploaded_file() to public/assets/uploads/, returns '/assets/uploads/'.name. THIS tuple contract MUST NOT change (called by handleUpload() AND uploadImages()).
  - handleUpload() (lines 450-463) and uploadImages() (lines 303-357) BOTH delegate to storeUploadedFile() — swapping storeUploadedFile()'s internals routes them through R2 automatically; do NOT edit those two methods.
  - deleteImage() (lines 360-387) — currently: imageModel->delete() (keep), then `$file = dirname(__DIR__,2).'/public'.$image['path']; if (is_file($file)) @unlink($file);` (REMOVE ENTIRELY), then cover-clear check `$product['image_path'] === $image['path']` (keep — both are now full URLs, comparison still valid).
  - MAX_UPLOAD_BYTES (line 446) = 2*1024*1024 — raise to (int)(3.5*1024*1024).
  - The cover auto-set in uploadImages() stores `$first['path']` (now a full URL) via setCover() — unchanged, works as-is (D-05).

App\Models\ProductImage::create(int $userId, int $productId, string $path): int
  - Stores the opaque `path` string verbatim — works unchanged with a full r2.dev URL (D-05). No model change.

R2 S3Client config (RESEARCH Pattern 1, locked by D-04) — copy exactly, do not redesign:
  version 'latest', region 'auto', endpoint 'https://'.Env::get('R2_ACCOUNT_ID').'.r2.cloudflarestorage.com',
  credentials new Aws\Credentials\Credentials(Env::get('R2_ACCESS_KEY_ID'), Env::get('R2_SECRET_ACCESS_KEY')).
  putObject params: Bucket, Key, SourceFile (tmp path), ContentType — NO ACL (R2 ignores x-amz-acl).
  deleteObject params: Bucket, Key. deriveKey: ltrim(substr($url, strlen($publicBaseUrl)), '/').
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add aws/aws-sdk-php to composer.json, raise upload limits in api/php.ini, document R2_* vars in .env.example</name>
  <files>composer.json, api/php.ini, .env.example</files>
  <read_first>
    - composer.json (full file — the require block has php + ext-pdo; the scripts block has only "test": "phpunit"; there is no "extra" block yet)
    - api/php.ini (the existing variables_order / memory_limit / disable_functions / session.gc_probability lines — append two new directives)
    - .env.example (the DB block + Aiven comment style to mirror for the R2 block)
    - .vercelignore (confirm /vendor is present — do NOT change it; D-02)
    - .planning/phases/04-image-storage-r2/04-RESEARCH.md → "composer.json additions", "api/php.ini additions", Pitfall 1 (.vercelignore /vendor scope), Pitfall 2 (--no-scripts), Pitfall 5 (silent $_FILES empty), "Open Questions #1" (vercel-php auto composer install, no buildCommand)
  </read_first>
  <action>
    Three mechanical config edits. Use the EXACT values from 04-RESEARCH — do not improvise versions or limits.
    1. **composer.json (D-01):** Add `"aws/aws-sdk-php": "^3"` to the `require` block (alongside the existing `"php": ">=8.3"` and `"ext-pdo": "*"`). In the existing `scripts` block, add `"vercel": "@composer dump-autoload --optimize"` (keep the existing `"test": "phpunit"`). Do NOT add `pre-autoload-dump`/`removeUnusedServices` and do NOT add an `extra.aws/aws-sdk-php` block: per RESEARCH Open Question #1 / Pitfall 2, vercel-php@0.7.4 runs `composer install --no-scripts`, so the `pre-autoload-dump` slimming hook never fires during the Vercel build — adding it would falsely imply the Lambda is slimmed when it is not. The full SDK (~37.5 MB) ships and fits within the 250 MB Lambda limit (RESEARCH Open Question #4) — this is the accepted approach. Do NOT add a `buildCommand` to vercel.json — vercel-php@0.7.4 auto-runs `composer install` on detecting composer.json; vercel.json is NOT touched by this plan. Do NOT commit `vendor/`; do NOT remove `/vendor` from `.vercelignore` (D-02 — the build regenerates vendor inside the Lambda regardless of the source-upload exclusion).
       - NOTE FOR EXECUTOR: do NOT run `composer require aws/aws-sdk-php` if the install is heavy/offline — edit the `require` block in composer.json directly. A regenerated `composer.lock` is fine if `composer` runs cleanly locally, but is not required for this autonomous plan (vercel-php resolves at build time). Never stage `vendor/`.
    2. **api/php.ini (D-07, STORE-05):** Append `upload_max_filesize = 5M` and `post_max_size = 5M` with a short comment explaining the layered defence (php.ini 5M > app guard 3.5 MB < Vercel raw 413 at 4.5 MB), so PHP does not silently empty `$_FILES` for a valid 3.x MB upload before the app-level guard fires (RESEARCH Pitfall 5). Leave `variables_order`, `memory_limit`, `disable_functions`, and `session.gc_probability` unchanged.
    3. **.env.example (D-09):** Add a commented `# Cloudflare R2 (image storage)` block documenting the 5 variables with NO secret values: `R2_ACCOUNT_ID=`, `R2_ACCESS_KEY_ID=`, `R2_SECRET_ACCESS_KEY=`, `R2_BUCKET=`, `R2_PUBLIC_BASE_URL=` (with an inline comment that R2_PUBLIC_BASE_URL is the `https://pub-<hash>.r2.dev` dev URL obtained after enabling public access — operator step in 04-02). Mirror the existing DB/Aiven comment style. No real values, no secrets (the access key + secret are set only in Vercel env vars + the operator's local .env).
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$c = Get-Content -Raw composer.json; if (($c -match 'aws/aws-sdk-php') -and ($c -match '\"vercel\"') -and ($c -notmatch 'removeUnusedServices')) { 'composer-ok' } else { exit 1 }"</automated>
    <automated>powershell -NoProfile -Command "try { Get-Content -Raw composer.json | ConvertFrom-Json | Out-Null; 'json-valid' } catch { exit 1 }"</automated>
    <automated>powershell -NoProfile -Command "$c = Get-Content -Raw api/php.ini; if (($c -match 'upload_max_filesize\s*=\s*5M') -and ($c -match 'post_max_size\s*=\s*5M')) { 'phpini-ok' } else { exit 1 }"</automated>
    <automated>powershell -NoProfile -Command "$c = Get-Content -Raw .env.example; if (($c -match 'R2_ACCOUNT_ID') -and ($c -match 'R2_ACCESS_KEY_ID') -and ($c -match 'R2_SECRET_ACCESS_KEY') -and ($c -match 'R2_BUCKET') -and ($c -match 'R2_PUBLIC_BASE_URL')) { 'env-ok' } else { exit 1 }"</automated>
    <automated>powershell -NoProfile -Command "if (Select-String -Path .vercelignore -Pattern '^/vendor' -Quiet) { 'vendor-ignored' } else { exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    - composer.json `require` contains `"aws/aws-sdk-php": "^3"`; `scripts` contains `vercel` (@composer dump-autoload --optimize) and still `test`; NO `pre-autoload-dump`/`removeUnusedServices` entry and NO `extra.aws/aws-sdk-php` block (they would falsely imply Lambda slimming that --no-scripts prevents); the file is valid JSON.
    - api/php.ini contains `upload_max_filesize = 5M` and `post_max_size = 5M`; the four pre-existing directives are unchanged.
    - .env.example documents all five `R2_*` keys with empty/no-secret values and a comment that R2_PUBLIC_BASE_URL is the r2.dev dev URL.
    - `/vendor` is still present in `.vercelignore` (unchanged); `vendor/` is not staged; vercel.json is not modified.
  </acceptance_criteria>
  <done>The SDK dependency + slimming scripts are declared, the PHP upload limits give headroom above the 3.5 MB app guard, and the R2_* env contract is documented — all without committing vendor or touching vercel.json.</done>
</task>

<task type="auto">
  <name>Task 2: Wire guarded vendor/autoload.php + CSP img-src r2.dev into public/index.php, and create the R2Storage service</name>
  <files>public/index.php, src/Services/R2Storage.php</files>
  <read_first>
    - public/index.php (lines 21-53 — the `$root = dirname(__DIR__)` line, the spl_autoload_register block, and the Content-Security-Policy header with `img-src 'self' data:`)
    - src/Services/ExchangeRateService.php (the Services convention: final class, declare(strict_types=1), namespace App\Services, no Core/Models deps, file-level PHPDoc, lazy instantiation by callers)
    - src/Core/Env.php is referenced only via Env::get() — no need to read it; the signature is in <interfaces>
    - .planning/phases/04-image-storage-r2/04-RESEARCH.md → "Pattern 1: R2Storage Service Class" (the EXACT class to implement — copy it), "Pattern 2: Guarded vendor/autoload.php" (the EXACT public/index.php edit), "S3Client instantiation for R2", "PutObject", "DeleteObject", "Key derivation", Pitfall 3 (no ACL), Pitfall 6 (no use_path_style_endpoint unless DNS fails), "Security Domain" (CSP note — overridden here)
  </read_first>
  <action>
    Two edits: wire Composer's autoloader + open the CSP for r2.dev in `public/index.php`, then create `src/Services/R2Storage.php` exactly per 04-RESEARCH Pattern 1. Interface-first: R2Storage is the contract Task 3 consumes, so it lands here before the controller swap.
    1. **public/index.php — vendor autoload (D-03, RESEARCH Pattern 2):** Immediately after `$root = dirname(__DIR__);` and BEFORE the `spl_autoload_register(...)` block, add the guarded require: `if (is_file($root . '/vendor/autoload.php')) { require $root . '/vendor/autoload.php'; }`. Composer handles `Aws\*`/`GuzzleHttp\*`; the maison `spl_autoload_register` handles `App\*` exclusively (it early-returns on non-`App\` classes) — no conflict, either order works, convention is Composer first. The `is_file()` guard keeps local Docker (no vendor) booting normally. Do NOT remove or reorder the existing spl_autoload_register or the `require $root.'/src/helpers.php'` line.
    2. **public/index.php — CSP img-src (orchestrator CSP override, RESEARCH risk #1 for STORE-02):** In the `Content-Security-Policy` header, change the `"img-src 'self' data:; "` directive to `"img-src 'self' data: https://*.r2.dev; "`. This is REQUIRED in this phase: the current policy BLOCKS r2.dev images, so STORE-02 ("uploaded images display in production") would be false without it. Add a short comment noting this pre-satisfies part of Phase 5 SEC-03. Leave the other CSP directives (default-src, script-src, style-src, font-src, connect-src) exactly as they are. Use the `https://*.r2.dev` wildcard (not a specific pub-hash) because the bucket/hash is created later by the operator in Wave 2 — the wildcard matches any `pub-<hash>.r2.dev` host.
    3. **src/Services/R2Storage.php (D-04, D-05 — RESEARCH Pattern 1, copy exactly):** Create a `final class R2Storage` with `declare(strict_types=1);`, `namespace App\Services;`, explicit `use` imports for `App\Core\Env`, `Aws\Credentials\Credentials`, `Aws\S3\S3Client` (and `Aws\Exception\AwsException` if referenced). File-level PHPDoc matching the Services convention. Constructor reads `R2_ACCOUNT_ID`, `R2_BUCKET`, `R2_PUBLIC_BASE_URL` (rtrim trailing '/') via `Env::get()` and builds the S3Client with `version => 'latest'`, `region => 'auto'`, `endpoint => 'https://'.$accountId.'.r2.cloudflarestorage.com'`, `credentials => new Credentials(Env::get('R2_ACCESS_KEY_ID'), Env::get('R2_SECRET_ACCESS_KEY'))`. NO `use_path_style_endpoint` (Pitfall 6 — add only as a fallback if DNS errors appear live). Methods: `put(string $tmpPath, string $key, string $contentType): string` → `putObject([Bucket, Key, SourceFile => $tmpPath, ContentType])` with NO ACL param (Pitfall 3), returns `$this->publicBaseUrl . '/' . $key`; `delete(string $keyOrUrl): void` → `deleteObject([Bucket, Key => deriveKey()])`; `private deriveKey(string $keyOrUrl): string` → `str_starts_with($keyOrUrl, $publicBaseUrl) ? ltrim(substr($keyOrUrl, strlen($publicBaseUrl)), '/') : ltrim($keyOrUrl, '/')`. Constructor takes NO parameters (D-04 locks the parameterless shape — reads Env). No Models/Core deps beyond Env.
  </action>
  <verify>
    <automated>php -l src/Services/R2Storage.php</automated>
    <automated>php -l public/index.php</automated>
    <automated>powershell -NoProfile -Command "$c = Get-Content -Raw public/index.php; $a = $c.IndexOf('vendor/autoload.php'); $s = $c.IndexOf('spl_autoload_register'); if ($a -ge 0 -and $s -ge 0 -and $a -lt $s -and ($c -match 'is_file\(\s*\$root\s*\.\s*.{0,4}/vendor/autoload\.php')) { 'autoload-before-spl' } else { exit 1 }"</automated>
    <automated>powershell -NoProfile -Command "$c = Get-Content -Raw public/index.php; if (($c -match 'img-src') -and ($c -match 'r2\.dev')) { 'csp-r2-ok' } else { exit 1 }"</automated>
    <automated>powershell -NoProfile -Command "$c = Get-Content -Raw src/Services/R2Storage.php; if (($c -match 'final class R2Storage') -and ($c -match 'declare\(strict_types=1\)') -and ($c -match 'namespace App\\Services') -and ($c -match \"region'?\s*=>\s*'auto'\") -and ($c -match 'r2\.cloudflarestorage\.com')) { 'r2storage-shape' } else { exit 1 }"</automated>
    <automated>powershell -NoProfile -Command "$c = Get-Content -Raw src/Services/R2Storage.php; if (($c -match 'function put') -and ($c -match 'function delete') -and ($c -match 'putObject') -and ($c -match 'deleteObject') -and ($c -notmatch \"'ACL'\")) { 'methods-no-acl' } else { exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    - `php -l` is clean on both `public/index.php` and `src/Services/R2Storage.php`.
    - public/index.php requires `vendor/autoload.php` guarded by `is_file($root . '/vendor/autoload.php')` and that require appears BEFORE `spl_autoload_register`; the existing autoloader + helpers require are unchanged.
    - The CSP `img-src` directive now includes `https://*.r2.dev` alongside `'self' data:`; no other CSP directive changed.
    - R2Storage is a `final class` in `namespace App\Services` with `declare(strict_types=1)`, S3Client configured with `region 'auto'` + the `r2.cloudflarestorage.com` endpoint + explicit Credentials, NO `use_path_style_endpoint`, NO ACL on putObject.
    - `put()` returns `publicBaseUrl . '/' . key`; `delete()` derives the key by stripping `publicBaseUrl`; the constructor takes no parameters and reads config via `Env::get()`.
  </acceptance_criteria>
  <done>Composer's autoloader loads before the maison one, the CSP allows r2.dev images (STORE-02 unblocked), and the R2Storage service exists with the exact research-locked S3Client config and put/delete/deriveKey surface.</done>
</task>

<task type="auto">
  <name>Task 3: Swap ProductController upload/delete from disk to R2 and raise the 3.5 MB size guard</name>
  <files>src/Controllers/ProductController.php</files>
  <read_first>
    - src/Controllers/ProductController.php → storeUploadedFile() (465-488), deleteImage() (360-387), handleUpload() (450-463), uploadImages() (303-357), MAX_UPLOAD_BYTES (446). Note: handleUpload() and uploadImages() BOTH delegate to storeUploadedFile() — they need NO edits.
    - src/Services/R2Storage.php (created in Task 2 — the put/delete contract this task consumes)
    - .planning/phases/04-image-storage-r2/04-RESEARCH.md → "Pattern 3: storeUploadedFile() R2 Swap" + "Pattern 4: deleteImage() R2 Swap" (copy these), Pitfall 4 (the old unlink block must be REMOVED, not guarded), Anti-Patterns (lazy R2Storage construction; store full URL not key; no @unlink on a URL)
  </read_first>
  <action>
    Surgical swap of disk I/O for R2 in `ProductController`, per 04-RESEARCH Patterns 3 & 4. Add `use App\Services\R2Storage;` to the imports (after the existing `use App\Services\ProfitCalculator;`). Instantiate R2Storage LAZILY inside each method (`new R2Storage()` — never in the constructor; matches the ExchangeRateService pattern, D-06 anti-pattern).
    1. **MAX_UPLOAD_BYTES (D-07, STORE-05):** Change `private const MAX_UPLOAD_BYTES = 2 * 1024 * 1024;` to `private const MAX_UPLOAD_BYTES = (int) (3.5 * 1024 * 1024);` and update the trailing comment to ~3,5 Mo.
    2. **storeUploadedFile() (D-06, Pattern 3):** Keep the size-guard check, the `finfo`/MIME allowlist (jpeg/png/webp/gif), and the `bin2hex(random_bytes(8)) . '.' . ext` key naming UNCHANGED. Update the over-limit message to a clear French string referencing ~3,5 Mo (STORE-05). Replace the `$dest = ...; if (!move_uploaded_file(...)) {...}; return ['/assets/uploads/'.$name, null];` tail with: in a `try`, `$url = (new R2Storage())->put($tmp, $name, (string) $mime);` and `return [$url, null];`; in `catch (\Throwable $e)`, `error_log('R2 put failed: ' . $e->getMessage());` and `return [null, 'échec de l\'enregistrement du fichier.'];`. The `[?string,?string]` tuple shape MUST stay identical — handleUpload() and uploadImages() depend on it and need NO edits. The returned path is now a full r2.dev URL (D-05) — ProductImage::create()/setCover() store it verbatim, no model/view change.
    3. **deleteImage() (D-06, D-08, Pattern 4, Pitfall 4 — CRITICAL):** Keep `$imageModel->delete((int) $image['id'], $userId);` (DB row deleted first). Then FULLY REMOVE the local-file block `$file = dirname(__DIR__, 2) . '/public' . $image['path']; if (is_file($file)) { @unlink($file); }` — delete it entirely, do not guard or comment it out (an `is_file()` on an `https://...r2.dev/...` URL is always false, so the old code would silently never delete the R2 object). Replace it with a best-effort R2 delete: `try { (new R2Storage())->delete((string) $image['path']); } catch (\Throwable $e) { error_log('R2 delete failed for path ' . $image['path'] . ': ' . $e->getMessage()); }`. Keep the subsequent cover-clear check `if ($product !== null && $product['image_path'] === $image['path']) { ... setCover(..., null); }` UNCHANGED (both sides are now full URLs; the comparison still holds). The request must succeed even if the R2 delete throws (D-08 best-effort).
    4. Do NOT touch handleUpload(), uploadImages(), setCoverImage(), validate(), or any other method. Do NOT change ProductImage, the views, or vercel.json.
  </action>
  <verify>
    <automated>php -l src/Controllers/ProductController.php</automated>
    <automated>powershell -NoProfile -Command "$c = Get-Content -Raw src/Controllers/ProductController.php; if (($c -match 'use App\\Services\\R2Storage;') -and ($c -match 'new R2Storage\(\)') -and ($c -match '->put\(') -and ($c -match '->delete\(')) { 'r2-wired' } else { exit 1 }"</automated>
    <automated>powershell -NoProfile -Command "$c = Get-Content -Raw src/Controllers/ProductController.php; if (($c -notmatch 'move_uploaded_file') -and ($c -notmatch '@unlink') -and ($c -notmatch \"/public.\s*\.\s*.\image..path\")) { 'disk-io-removed' } else { exit 1 }"</automated>
    <automated>powershell -NoProfile -Command "$c = Get-Content -Raw src/Controllers/ProductController.php; if ($c -match 'MAX_UPLOAD_BYTES\s*=\s*\(int\)\s*\(3\.5\s*\*\s*1024\s*\*\s*1024\)') { 'guard-3point5' } else { exit 1 }"</automated>
    <automated>powershell -NoProfile -Command "$c = Get-Content -Raw src/Controllers/ProductController.php; if (($c -match 'function handleUpload') -and ($c -match 'function uploadImages') -and ($c -match 'function storeUploadedFile')) { 'methods-intact' } else { exit 1 }"</automated>
    <automated>powershell -NoProfile -Command "if (Test-Path vendor/bin/phpunit) { vendor/bin/phpunit } else { 'phpunit-skipped-no-vendor (regression unaffected: no Services business logic changed)' }"</automated>
  </verify>
  <acceptance_criteria>
    - `php -l src/Controllers/ProductController.php` is clean.
    - `use App\Services\R2Storage;` is imported; `storeUploadedFile()` calls `(new R2Storage())->put($tmp, $name, (string) $mime)` (explicit `(string)` cast — `finfo_file()` returns `string|false`, required under `declare(strict_types=1)`) in a try/catch and returns the full URL in the same `[?string,?string]` tuple; the MIME allowlist + random key naming are unchanged.
    - `deleteImage()` calls `(new R2Storage())->delete((string) $image['path'])` best-effort (try/catch + error_log) AFTER the DB row delete; the old `$file = dirname(__DIR__,2).'/public'.$image['path']` / `is_file` / `@unlink` block is GONE entirely.
    - `move_uploaded_file`, `@unlink`, and the `/public . $image['path']` concatenation no longer appear anywhere in the file.
    - `MAX_UPLOAD_BYTES = (int)(3.5 * 1024 * 1024)` with a clear French ~3,5 Mo over-limit message; handleUpload()/uploadImages()/storeUploadedFile() all still present and not otherwise edited.
    - The existing `vendor/bin/phpunit` (ProfitCalculator) suite remains green — no Services business logic was altered.
  </acceptance_criteria>
  <done>Product image upload and delete go through R2 (full r2.dev URL stored, object removed on delete), the size guard is raised to 3.5 MB with a clear French message, and the gallery/cover paths ride the change with zero edits — no disk I/O remains.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser multipart upload → PHP function | Untrusted file bytes + filename + size cross here; MIME and size must be validated server-side |
| PHP function → Cloudflare R2 | Object writes/deletes over the S3 API, authenticated by the R2 API token (env vars only) |
| Stored r2.dev URL → browser `<img>` | The public r2.dev URL is rendered into the page; CSP `img-src` must allow it |
| Vercel build → Lambda vendor/ | `aws/aws-sdk-php` is resolved at build time; supply-chain trust in the package |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-01 | Tampering | MIME-type spoofing (upload .php disguised as .jpg) | mitigate | `finfo_file(FILEINFO_MIME_TYPE)` reads real bytes against the jpeg/png/webp/gif allowlist in storeUploadedFile() (kept unchanged, Task 3); extension is derived from the detected MIME, not user input. |
| T-4-02 | Denial of Service | Oversized upload (Lambda OOM / raw 413) | mitigate | Layered guard: api/php.ini `upload_max_filesize=5M`+`post_max_size=5M` (Task 1) → app `MAX_UPLOAD_BYTES=3.5MB` clear-message guard (Task 3) → Vercel 4.5 MB raw 413 (documented, out of PHP scope). |
| T-4-03 | Information Disclosure | R2 credential leak via committed config | mitigate | All five `R2_*` read via `Env::get()` only; `.env.example` carries keys with no values (Task 1); secrets live in Vercel env vars + the operator's gitignored local `.env`; `vendor/` and `.env` not committed (D-02/SEC-01). |
| T-4-04 | Tampering | Path traversal via the object key | mitigate | Key is `bin2hex(random_bytes(8)) . '.' . <allowlisted-ext>` — no user-controlled segment; 2^64 keyspace also defeats enumeration (RESEARCH Security Domain). |
| T-4-05 | Information Disclosure | R2 SDK error details leaked to the user | mitigate | put/delete failures are `error_log()`-ed server-side; the user sees a generic French message ("échec de l'enregistrement du fichier.") — no R2/SDK internals surfaced (Task 3, V7). |
| T-4-06 | Spoofing | Stored r2.dev image origin blocked/abused by CSP | mitigate | CSP `img-src` is tightened to `'self' data: https://*.r2.dev` (Task 2) — only the r2.dev image origin is permitted, no broad `*`. Pre-satisfies part of Phase 5 SEC-03. |
| T-4-SC | Tampering | composer install of `aws/aws-sdk-php` | mitigate | RESEARCH "Package Legitimacy Audit": `aws/aws-sdk-php` is `[OK]` / Approved (official AWS SDK, ~12 yrs, 536M installs, slopcheck OK on Packagist). Pinned `^3`; no `[ASSUMED]`/`[SUS]` packages → no blocking-human legitimacy checkpoint required. |
</threat_model>

<verification>
- STORE-01 (code): R2Storage::put() issues `putObject` with Bucket/Key/SourceFile/ContentType and returns `R2_PUBLIC_BASE_URL.'/'.key`; storeUploadedFile() returns that URL (not `/assets/uploads/...`). Wave 1 gate = `php -l` clean + structural greps (no live R2 yet).
- STORE-02 (code half): CSP `img-src` includes `https://*.r2.dev`; the full URL is stored and rendered by existing views unchanged (D-05). Live display proof is in 04-02.
- STORE-03 (code): deleteImage() calls R2Storage::delete() best-effort; deriveKey() strips R2_PUBLIC_BASE_URL; the old `@unlink` block is gone (grep asserts absence). Live 404 proof is in 04-02.
- STORE-04: documented fallback — prod DB empty, no migration (D-08); confirmed live by the operator in 04-02.
- STORE-05: MAX_UPLOAD_BYTES = (int)(3.5*1024*1024) + clear FR message; api/php.ini 5M limits. Live >3.5 MB rejection proof is in 04-02.
- Regression: `vendor/bin/phpunit` (ProfitCalculator) stays green — no Services business logic altered.

NOTE (Nyquist): behavioral unit tests for R2Storage (put/delete/deriveKey) would require mocking `Aws\S3\S3Client`, but D-04 locks a parameterless constructor (no injection point) and REQUIREMENTS.md scopes extended controller/Service-integration tests OUT ("seul ProfitCalculator reste testé"; phpunit coverage source = `src/Services/` business logic only). Following the Plan 03-01 precedent, Wave 1 automated gates are `php -l` lint + structural presence/absence greps; end-to-end STORE-01..05 behavior is verified LIVE against R2 in Wave 2 (Plan 04-02). The deferred test gap (`tests/Services/R2StorageTest.php`) is recorded in 04-VALIDATION.md.
</verification>

<success_criteria>
- `aws/aws-sdk-php ^3` is declared in composer.json with the Vercel slimming scripts; `vendor/` is not committed and `/vendor` stays in `.vercelignore` (D-01, D-02).
- public/index.php loads `vendor/autoload.php` (guarded) before the maison autoloader and the CSP `img-src` allows `https://*.r2.dev` (D-03 + STORE-02 unblock).
- `final class R2Storage` wraps `Aws\S3\S3Client` with the research-locked R2 config and the put/delete/deriveKey surface (D-04, D-05).
- ProductController upload/delete go through R2 with the 3.5 MB guard + clear FR message; no `move_uploaded_file`/`@unlink` remains (D-06, D-07, STORE-01/03/05).
- api/php.ini upload limits + `.env.example` R2_* docs are in place (D-07, D-09); STORE-04 fallback documented (D-08).
- `php -l` clean on every modified PHP file; `vendor/bin/phpunit` green; no secret committed.
</success_criteria>

<output>
Create `.planning/phases/04-image-storage-r2/04-01-SUMMARY.md` when done — record the composer.json additions, the exact public/index.php edits (vendor autoload + CSP img-src), the R2Storage class shape, the ProductController swap (storeUploadedFile/deleteImage/MAX_UPLOAD_BYTES), the api/php.ini limits, the .env.example R2_* block, and confirmation that vendor/ was not committed.
</output>
