---
phase: 02-database-and-schema-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Core/Database.php
  - .env.example
  - bin/migrate.php
  - certs/.gitkeep
  - certs/README.md
autonomous: true
requirements: [DB-01, DB-02, DB-03]

must_haves:
  truths:
    - "Database::connection() adds PDO::MYSQL_ATTR_SSL_CA and MYSQL_ATTR_SSL_VERIFY_SERVER_CERT only when the resolved CA cert file exists (local Docker without the cert still connects)"
    - "Database::connection() no longer calls Schema::ensure() — no DDL is executed at runtime (DB-03)"
    - "bin/migrate.php exists, is CLI-guarded, applies sql/schema.sql (stripping CREATE DATABASE / USE) then calls Schema::ensure(), and exits 0 on success / 1 on failure"
    - "bin/migrate.php never loads sql/seed.sql (D-08)"
    - ".env.example documents DB_SSL_CA and the Aiven connection variables without committing any secret"
    - "certs/ is tracked in git and is NOT excluded by .vercelignore"
  artifacts:
    - path: "src/Core/Database.php"
      provides: "TLS PDO options (cert-file-guarded) + Schema::ensure() removed from the request path"
      contains: "MYSQL_ATTR_SSL_CA"
    - path: "bin/migrate.php"
      provides: "One-shot operator migration: schema.sql then Schema::ensure(), idempotent and re-runnable"
      min_lines: 50
    - path: ".env.example"
      provides: "DB_SSL_CA variable + documented Aiven connection vars"
      contains: "DB_SSL_CA"
    - path: "certs/README.md"
      provides: "Operator instructions for placing the committed Aiven CA cert"
  key_links:
    - from: "src/Core/Database.php"
      to: "certs/aiven-ca.pem"
      via: "PDO::MYSQL_ATTR_SSL_CA pointing at the resolved cert path"
      pattern: "MYSQL_ATTR_SSL_CA"
    - from: "bin/migrate.php"
      to: "App\\Core\\Schema::ensure"
      via: "require + call after applying schema.sql"
      pattern: "Schema::ensure"
    - from: "bin/migrate.php"
      to: "sql/schema.sql"
      via: "file_get_contents + per-statement exec"
      pattern: "schema\\.sql"
---

<objective>
Make the two coupled code changes that let ResellTrack reach Aiven MySQL 8 over TLS and apply its schema out of the request path, without any live Aiven service yet. (1) Add cert-guarded TLS options to `Database::connection()` and remove the per-request `Schema::ensure()` call (DB-01 + DB-03, code half). (2) Create the one-shot `bin/migrate.php` operator script and the `certs/` scaffolding (DB-02, code half).

Purpose: This is the entirely-autonomous half of Phase 2 — pure PHP + config the agent can complete and verify locally. Wave 2 (Plan 02-02) is the operator half: provision Aiven, commit the CA cert, set env vars, run the migration, and verify on the live URL.

Output: Modified `src/Core/Database.php`, modified `.env.example`, new `bin/migrate.php`, and `certs/` scaffolding (`.gitkeep` + `README.md`). No secret is committed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-database-and-schema-migration/02-CONTEXT.md
@.planning/phases/02-database-and-schema-migration/02-RESEARCH.md

<!-- Files this plan reads/modifies -->
@src/Core/Database.php
@src/Core/Schema.php
@src/Core/Env.php
@public/index.php

<interfaces>
<!-- Contracts the executor needs — already in the codebase, no exploration required. -->

App\Core\Env::get(string $key, ?string $default = null): ?string
  - Real environment variables (e.g. Vercel injection, docker-compose `environment:`) win over .env file values.

App\Core\Env::load(string $path): void
  - Loads a .env file; safe no-op if the file is absent.

App\Core\Database::connection(): PDO
  - Singleton. Currently (line 41) calls Schema::ensure(self::$instance) after `new PDO(...)`. DB-03 removes that call.
  - Existing $options keep: ATTR_ERRMODE => ERRMODE_EXCEPTION, ATTR_DEFAULT_FETCH_MODE => FETCH_ASSOC, ATTR_EMULATE_PREPARES => false.
  - Existing catch (PDOException) friendly-500 block (lines 42-48) MUST stay unchanged (D-10).

App\Core\Schema::ensure(PDO $db): void
  - Idempotent structural DDL (CREATE TABLE IF NOT EXISTS, SHOW COLUMNS guards). Reused as-is by bin/migrate.php (D-01/D-02). Do NOT modify this file.

public/index.php (lines 24-36)
  - The spl_autoload_register App\ -> src/ pattern + `require $root . '/src/helpers.php';` that bin/migrate.php must mirror inline. bin/migrate.php must NOT require public/index.php.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add cert-guarded TLS options to Database::connection() and remove the per-request Schema::ensure() call</name>
  <files>src/Core/Database.php, .env.example</files>
  <read_first>
    - src/Core/Database.php (the file being modified — note the $options array at lines 33-37, the `new PDO` + `Schema::ensure` at lines 40-41, and the catch block at lines 42-48)
    - src/Core/Env.php (Env::get default-fallback behavior)
    - .planning/phases/02-database-and-schema-migration/02-RESEARCH.md → "Pattern 1: PDO SSL Options for Aiven MySQL", "Code Examples → Complete Database.php TLS Modification", "Runtime State Inventory" (Option 3 / is_file guard), and Pitfall 2 (relative cert path)
    - .env.example (the file being modified)
  </read_first>
  <action>
    Modify `Database::connection()` per D-03/D-04/D-05 and the is_file guard mandated by 02-RESEARCH (Runtime State Inventory, Option 3). Use the exact identifiers and resolution logic from RESEARCH "Complete Database.php TLS Modification" — do not invent alternatives:
    1. After the DSN is built and before `new PDO(...)`, read the cert path: `Env::get('DB_SSL_CA', 'certs/aiven-ca.pem')`. Resolve a relative value to an absolute path anchored to the file (not the cwd) using `dirname(__FILE__, 3) . '/' . $relCert`; treat a value beginning with `/` as already absolute (`str_starts_with`). This avoids Pitfall 2 (relative cert path fails under differing cwd between CLI and Lambda).
    2. Add `PDO::MYSQL_ATTR_SSL_CA => $certPath` and `PDO::MYSQL_ATTR_SSL_VERIFY_SERVER_CERT => true` to the `$options` array ONLY when `is_file($certPath)` is true. When the cert file is absent (local Docker dev without the committed cert), apply NO SSL options so the plaintext connection still works — this preserves the local-dev-unchanged core value. Do NOT use `MYSQL_ATTR_SSL_VERIFY_SERVER_CERT => false` anywhere (D-05). Do NOT put `sslmode=...` in the DSN string (that is PostgreSQL syntax; pdo_mysql silently ignores it — see RESEARCH Anti-Patterns / Pitfall 1).
    3. Keep the three existing options (ATTR_ERRMODE, ATTR_DEFAULT_FETCH_MODE, ATTR_EMULATE_PREPARES) unchanged.
    4. Remove the line `Schema::ensure(self::$instance);` (currently line 41) so a runtime connection performs connection only — no DDL (DB-03). Leave the `try { self::$instance = new PDO(...); } catch (PDOException $e) { ... }` friendly-500 block otherwise unchanged (D-10).
    5. In `.env.example`, under the existing `# Database` section, add `DB_SSL_CA=certs/aiven-ca.pem` with a one-line comment (path relative to project root or absolute). Also add a clearly-commented documentation block (no real values) noting that the production Aiven connection (`DB_HOST`, `DB_PORT`, `DB_NAME`, `DB_USER`, `DB_PASSWORD`) is supplied via Vercel env vars + the operator's local `.env`, and that `DB_NAME` must match the actual Aiven database name (often `defaultdb`). Leave the existing Docker dev values (`DB_HOST=db`, etc.) intact so local dev is unchanged. Commit no secret.

    NOTE on the local-dev regression risk: once the operator commits `certs/aiven-ca.pem` in Wave 2, the cert file becomes present in local clones too. If a local Docker MySQL negotiates verifiable TLS, the `is_file`-guarded SSL options could attempt verification against Aiven's CA. RESEARCH assumption A3 rates this low-risk (Docker MySQL ignores the option); Plan 02-02 includes an explicit local-Docker regression check after the cert lands. Documented fallback if that check fails: gate the SSL options on `Env::get('DB_SSL_CA')` being explicitly set in the environment (Docker does not set it) rather than on file presence. Do not implement the fallback now — implement the is_file guard exactly as specified.
  </action>
  <verify>
    <automated>php -l src/Core/Database.php</automated>
    <automated>powershell -NoProfile -Command "if (Select-String -Path src/Core/Database.php -Pattern 'MYSQL_ATTR_SSL_CA' -Quiet) { 'SSL_CA-present' } else { exit 1 }"</automated>
    <automated>powershell -NoProfile -Command "if (Select-String -Path src/Core/Database.php -Pattern 'is_file' -Quiet) { 'cert-guard-present' } else { exit 1 }"</automated>
    <automated>powershell -NoProfile -Command "if (-not (Select-String -Path src/Core/Database.php -Pattern 'Schema::ensure' -Quiet)) { 'no-runtime-ensure' } else { exit 1 }"</automated>
    <automated>powershell -NoProfile -Command "if (-not (Select-String -Path src/Core/Database.php -Pattern 'VERIFY_SERVER_CERT\s*=>\s*false' -Quiet)) { 'verify-on' } else { exit 1 }"</automated>
    <automated>powershell -NoProfile -Command "if (Select-String -Path .env.example -Pattern 'DB_SSL_CA' -Quiet) { 'env-documented' } else { exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    - `php -l src/Core/Database.php` reports "No syntax errors detected".
    - `src/Core/Database.php` contains `PDO::MYSQL_ATTR_SSL_CA` and `PDO::MYSQL_ATTR_SSL_VERIFY_SERVER_CERT => true`, both added inside an `is_file($certPath)` guard.
    - `src/Core/Database.php` contains NO occurrence of `Schema::ensure` (DB-03) and NO `MYSQL_ATTR_SSL_VERIFY_SERVER_CERT => false`.
    - The cert path is resolved via `dirname(__FILE__, 3)` (absolute), not passed as a bare relative string.
    - The existing `catch (PDOException $e)` friendly-500 block is byte-for-byte unchanged (D-10).
    - `.env.example` contains `DB_SSL_CA=certs/aiven-ca.pem` and documents the Aiven vars; it contains no real credential values; `.env` itself is not added or modified.
  </acceptance_criteria>
  <done>Runtime connections use TLS only when the CA cert is present, never run DDL, and keep the friendly 500; `.env.example` documents the new TLS/Aiven config without leaking secrets.</done>
</task>

<task type="auto">
  <name>Task 2: Create bin/migrate.php and the certs/ scaffolding; confirm certs/ is not Vercel-excluded</name>
  <files>bin/migrate.php, certs/.gitkeep, certs/README.md</files>
  <read_first>
    - public/index.php (lines 21-39 — the autoloader + helpers + Env::load pattern to mirror inline)
    - src/Core/Schema.php (the `ensure(PDO)` entry point bin/migrate.php calls second; do not modify)
    - sql/schema.sql (lines 1-9 — the leading `SET NAMES` / `SET time_zone` plus the `CREATE DATABASE IF NOT EXISTS` / `USE` lines that must be stripped)
    - .vercelignore (confirm `certs/` is NOT listed — currently excludes /vendor /.git /docker /docker-compose.yml /Dockerfile /.planning /tests /sql .env .env.example)
    - .planning/phases/02-database-and-schema-migration/02-RESEARCH.md → "Pattern 2: bin/migrate.php Bootstrap", "Pattern 3: Executing schema.sql in migrate.php", "Code Examples → Complete bin/migrate.php", "Schema.sql vs Schema.php Reconciliation" (mandatory order), Pitfall 3 (CREATE DATABASE privilege), Pitfall 5 (no seed in prod), Pitfall 6 (order)
  </read_first>
  <action>
    Create `bin/migrate.php` as a new file in a new `bin/` directory, following 02-RESEARCH "Complete bin/migrate.php" exactly. Use these concrete elements (reference RESEARCH for the precise code — do not paste a divergent version):
    1. `declare(strict_types=1);` then a CLI guard: if `PHP_SAPI !== 'cli'`, `http_response_code(403); exit('CLI only.');` — bin/ ships in the Lambda bundle (not in .vercelignore), so this guard prevents web invocation (D-07, RESEARCH Open Q4).
    2. `$root = dirname(__DIR__);` then inline the `spl_autoload_register` for the `App\` -> `src/` prefix exactly as in public/index.php, and `require $root . '/src/helpers.php';`. Do NOT `require public/index.php` (it starts the session, sets HTTP headers, and dispatches the router — RESEARCH Anti-Patterns).
    3. `use App\Core\Env; use App\Core\Database; use App\Core\Schema;` then `Env::load($root . '/.env');`.
    4. Call `Database::connection()` to obtain the PDO handle (reuses the TLS options + friendly error from Task 1 — D-10).
    5. Read `sql/schema.sql` via `file_get_contents`. Split on `;`, `trim` each, `array_filter` out empties AND any statement matching `/^\s*(CREATE\s+DATABASE\b|USE\s+\w)/i` (Aiven pre-creates the DB and the operator may lack CREATE DATABASE privilege — Pitfall 3; the DSN already selects the database). The leading `SET NAMES` / `SET time_zone` statements are harmless session settings and execute fine — do not special-case them.
    6. Execute each remaining statement with `$pdo->exec($stmt)` inside a try/catch; on `PDOException`, echo the error + the offending statement and `exit(1)`. (Multi-statement single-exec is impossible with EMULATE_PREPARES=false — RESEARCH Anti-Patterns.)
    7. After all schema.sql statements succeed, call `Schema::ensure($pdo)` (schema.sql FIRST, Schema::ensure SECOND — mandatory order, Pitfall 6: reversing fails on FKs referencing users/products). Wrap in try/catch → exit(1) on Throwable.
    8. Echo human-readable progress ("Connecting...", "Applying sql/schema.sql...", "schema.sql applied (N statements).", "Applying Schema::ensure()...", "Migration complete.") and `exit(0)` on success.
    9. The script MUST only ever read `sql/schema.sql` — never `sql/seed.sql` (D-08, Pitfall 5).

    Create `certs/.gitkeep` (empty file) so the directory is tracked before the operator drops the cert in. Create `certs/README.md` documenting: the operator downloads the Aiven CA certificate from Aiven Console > Service > Overview (Download CA cert), saves it as `certs/aiven-ca.pem`, and commits it (D-04 — the CA is public information, not a secret; `.gitignore` does not exclude `*.pem`, verified). Note that `Database::connection()` defaults `DB_SSL_CA` to `certs/aiven-ca.pem`, and that `certs/` must remain out of `.vercelignore` so the cert ships in the Lambda bundle (D-06).

    Confirm (do not modify) that `.vercelignore` does NOT exclude `certs/`. If — and only if — it somehow does, that is a blocker to surface, not silently fix.
  </action>
  <verify>
    <automated>php -l bin/migrate.php</automated>
    <automated>powershell -NoProfile -Command "if (-not (Select-String -Path bin/migrate.php -Pattern 'seed' -Quiet)) { 'no-seed' } else { exit 1 }"</automated>
    <automated>powershell -NoProfile -Command "if ((Select-String -Path bin/migrate.php -Pattern 'PHP_SAPI' -Quiet) -and (Select-String -Path bin/migrate.php -Pattern 'Schema::ensure' -Quiet)) { 'guarded+ensure' } else { exit 1 }"</automated>
    <automated>powershell -NoProfile -Command "if (-not (Select-String -Path .vercelignore -Pattern '^/?certs' -Quiet)) { 'certs-not-excluded' } else { exit 1 }"</automated>
    <automated>powershell -NoProfile -Command "if ((Test-Path certs/.gitkeep) -and (Test-Path certs/README.md)) { 'scaffolding-present' } else { exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    - `php -l bin/migrate.php` reports "No syntax errors detected".
    - `bin/migrate.php` has a `PHP_SAPI !== 'cli'` guard, inlines the `spl_autoload_register` (does NOT require public/index.php), calls `Database::connection()`, applies `sql/schema.sql` with `CREATE DATABASE` / `USE` statements stripped, then calls `Schema::ensure($pdo)` in that order, and exits 0 on success / 1 on any statement error.
    - `bin/migrate.php` contains no reference to `seed` / `sql/seed.sql` (D-08).
    - `certs/.gitkeep` and `certs/README.md` exist; README documents downloading + committing `certs/aiven-ca.pem` from the Aiven Console.
    - `.vercelignore` does NOT contain a `certs` entry (cert ships in the Lambda bundle — D-06).
    - `src/Core/Schema.php` is unchanged (D-01).
  </acceptance_criteria>
  <done>An operator can run `php bin/migrate.php` from a machine with a populated `.env`; the certs/ directory is tracked and ready for the committed CA cert; the cert will ship to the Lambda.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App (Lambda / operator CLI) → Aiven MySQL | DB traffic crosses the public internet; must be TLS with server-cert verification |
| Repo → git history | Only the public CA cert may be committed; DB credentials must never enter the repo |
| bin/ in Lambda bundle → web | bin/migrate.php ships to Lambda; must not be web-invokable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-01 | Spoofing / Information Disclosure | DB connection (MITM) | mitigate | `PDO::MYSQL_ATTR_SSL_VERIFY_SERVER_CERT => true` with `MYSQL_ATTR_SSL_CA` validates Aiven's cert against the committed CA (D-05). Never set verify=false. |
| T-2-02 | Information Disclosure | Secrets in source | mitigate | `.env.example` carries no real values; only the public CA cert is committed (D-04). DB creds live in `.env` (gitignored) + Vercel env vars. Task 1 acceptance asserts no credential values. |
| T-2-03 | Tampering / Integrity | seed.sql reaching production | mitigate | bin/migrate.php only ever reads `sql/schema.sql`; Task 2 acceptance asserts no `seed` reference (D-08). |
| T-2-04 | Elevation of Privilege | bin/migrate.php invoked over HTTP on Lambda | mitigate | `PHP_SAPI !== 'cli'` guard returns 403 and exits before any DB access (D-07). |
| T-2-SC | Tampering | npm/composer package installs | accept | No package installs this phase — pure PHP + config (RESEARCH: Package Legitimacy Audit "not applicable"). No legitimacy checkpoint required. |
</threat_model>

<verification>
- DB-01 (code): `Database::connection()` adds TLS options guarded by `is_file($certPath)`; `php -l` clean; `MYSQL_ATTR_SSL_CA` present, `verify=false` absent.
- DB-02 (code): `bin/migrate.php` exists, CLI-guarded, applies schema.sql (stripped) then `Schema::ensure()`, never touches seed.sql; `php -l` clean.
- DB-03 (code): no `Schema::ensure` reference remains in `src/Core/Database.php`.
- Regression: existing `src/Core/Schema.php` unchanged; `vendor/bin/phpunit` (ProfitCalculator suite) remains green — no business-logic surface touched.
</verification>

<success_criteria>
- TLS PDO options are present and cert-guarded; runtime connections execute no DDL (DB-01 + DB-03, code level).
- `bin/migrate.php` is a runnable, idempotent, CLI-only one-shot migration that applies schema.sql then Schema::ensure() and never loads seed.sql (DB-02, code level).
- `certs/` is tracked and not Vercel-excluded, ready for the operator's committed CA cert.
- No secret is committed.
</success_criteria>

<output>
Create `.planning/phases/02-database-and-schema-migration/02-01-SUMMARY.md` when done — record the exact Database.php edits, the bin/migrate.php structure, and the `.env.example` / certs scaffolding additions.
</output>
